

\chapter{Desarrollo e Implementación}
\label{cap:desarrolloImplementacion}

Este capítulo presenta la implementación del sistema, trasladando al
código y al esquema de base de datos las decisiones de diseño expuestas
en el Capítulo~\ref{cap:arquitecturaMetodologia}. Su organización sigue
un orden ascendente, de los componentes que sostienen al resto a las
extensiones que aportan funcionalidad específica.

La exposición comienza por el modelo de datos
(Sección~\ref{sec:modeloDatos}), que sustenta tanto la persistencia en
PostgreSQL como la representación en memoria del estado de exploración.
Sobre esa base, la clase \texttt{State} y su gestor,
\texttt{StateManager} (Sección~\ref{sec:representacionContexto}),
articulan el contexto de un usuario en cada momento de la sesión. La
Sección~\ref{sec:filtrado} describe los mecanismos de filtrado clásico
y relacional, junto con la construcción dinámica de las consultas SQL
parametrizadas que traducen estos mecanismos y su contexto al motor de base de datos. A partir
de ese mecanismo común se introducen las dos extensiones que
constituyen el aporte diferencial del sistema: la navegación
transversal entre colecciones
(Sección~\ref{sec:navegacion_transversal}) y las operaciones de unión
sobre contextos independientes
(Sección~\ref{sec:operaciones_conjuntos}). El capítulo cierra con una
revisión de los obstáculos surgidos durante el desarrollo y las
soluciones adoptadas en cada caso (Sección~\ref{sec:obstaculos}).

\section{Modelo de datos}
\label{sec:modeloDatos}

La aplicación maneja dos representaciones complementarias de la
información. La primera es un esquema relacional persistente, alojado
en PostgreSQL, en el que se almacenan las entidades importadas de las
fuentes externas (TMDB, DBLP, etc.) junto con sus metadatos y vínculos.
El detalle del pipeline de integración con cada fuente (descarga,
procesado e ingesta) se documenta en el
Apéndice~\ref{Appendix:integracion}.
La segunda es una representación en memoria del contexto de exploración
de cada usuario, descrita en la
Sección~\ref{sec:representacionContexto}, que se traduce dinámicamente
a consultas sobre el esquema persistente.

Esta sección se centra en la primera capa. Antes de detallar el esquema
de tablas conviene partir de su contraparte conceptual, ya que el
modelo Entidad-Relación captura las abstracciones que el esquema
relacional se limita a codificar.

    \subsection{Modelo Entidad-Relación}

El modelo conceptual se articula en torno a cuatro entidades fuertes
(\textit{Dataset}, \textit{Collection}, \textit{Entity} y
\textit{Metadata}) y una relación N--N reflexiva sobre \textit{Entity},
dotada de atributo propio, que materializa los vínculos tipados entre
entidades.

Un \textit{Dataset} representa una fuente de datos completa, como el
catálogo de TMDB o un volcado de DBLP, y agrupa varias
\textit{Collections}, que son los tipos de entidad expuestos por esa
fuente. En el caso de TMDB, esas colecciones son \textit{Movies},
\textit{People}, \textit{Studios}, \textit{TV Series} y
\textit{Networks}. Cada \textit{Entity} pertenece a una única
\textit{Collection}, y a esa entidad se asocian dos conjuntos abiertos
de información: por un lado, una colección de \textit{Metadata} formada
por pares clave-valor que describen atributos del dominio (género, año
de estreno, idioma original, etc.); por otro, un número arbitrario de
\textit{references} hacia otras entidades, etiquetadas con una
\textit{reason} que indica el tipo de relación que las une
(\textit{Director}, \textit{Actor}, \textit{Network}, etc.). La
Figura~\ref{fig:erModel} resume estas entidades y las cardinalidades
entre ellas.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      every node/.style={font=\small},
      entity/.style={draw, rectangle, rounded corners=2pt,
                     minimum height=0.9cm, minimum width=2.4cm,
                     fill=blue!10},
      rel/.style={draw, diamond, aspect=2, fill=orange!15, inner sep=1pt},
      attr/.style={draw, ellipse, fill=yellow!20, inner sep=2pt,
                   font=\footnotesize\itshape},
      line/.style={thick}
    ]
    \node[entity] (ds)  {Dataset};
    \node[rel,    right=0.9cm of ds]  (r1)  {contiene};
    \node[entity, right=0.9cm of r1]  (col) {Collection};
    \node[rel,    below=0.9cm of col] (r2)  {agrupa};
    \node[entity, below=0.9cm of r2]  (ent) {Entity};

    \node[rel,    right=0.9cm of ent] (r3) {describe};
    \node[entity, right=0.9cm of r3]  (md) {Metadata};

    \node[rel, below=1.4cm of ent]    (r4) {reference};
    \node[attr, below=0.5cm of r4]    (rs) {reason};

    \draw[line] (ds)  -- node[above]{1} (r1);
    \draw[line] (r1)  -- node[above]{N} (col);
    \draw[line] (col) -- node[right]{1} (r2);
    \draw[line] (r2)  -- node[right]{N} (ent);
    \draw[line] (ent) -- node[above]{1} (r3);
    \draw[line] (r3)  -- node[above]{N} (md);

    % Relación reflexiva N--N sobre Entity con atributo reason
    \draw[line] (ent) to[out=-110, in=110, looseness=1.3]
        node[left,pos=0.5]{N (source)} (r4);
    \draw[line] (ent) to[out=-70, in=70, looseness=1.3]
        node[right,pos=0.5]{N (target)} (r4);
    \draw[line] (r4) -- (rs);
  \end{tikzpicture}
  \caption{Diagrama Entidad-Relación del modelo de datos persistente.}
  \label{fig:erModel}
\end{figure}

La relación \textit{reference} merece una observación específica. No
es una asociación N--N pura, sino una relación con atributo propio, ya
que cada vínculo lleva consigo la \textit{reason} que lo etiqueta. Este
matiz, aparentemente menor en la fase conceptual, condiciona
directamente la forma del esquema relacional: una relación con atributo
no admite la simplificación habitual a una tabla de cruce y exige una
tabla independiente, como se verá a continuación.

    \subsection{Modelo relacional}

La traducción del modelo conceptual a PostgreSQL distribuye estas
entidades en cinco tablas: una por cada entidad fuerte y una adicional
para la relación \textit{reference}, que por las razones recién
expuestas requiere existencia propia. La
Figura~\ref{fig:relationalModel} muestra el esquema implementado, con
las claves primarias subrayadas y las claves foráneas en itálica. Para
no recargar el diagrama, las restricciones de unicidad y los índices
secundarios se omiten en la figura y se comentan más adelante, ya que
forman parte de las decisiones de diseño que motivan este esquema.

\begin{figure}[H]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      font=\footnotesize\ttfamily,
      tblh/.style={fill=blue!25, draw, font=\footnotesize\bfseries,
                   minimum height=0.55cm, inner sep=4pt, align=center,
                   text width=3.6cm},
      tbl/.style={draw, rectangle, fill=blue!5, inner sep=4pt,
                  align=left, text width=3.6cm},
      fk/.style={-Stealth, thick, blue!60!black}
    ]
    % datasets (arriba a la izquierda)
    \node[tblh] (Hds) at (0,0) {datasets};
    \node[tbl, below=-0.4pt of Hds.south, anchor=north] (Bds) {%
      \underline{id} : SERIAL \\
      name : VARCHAR \\
      created\_at : TIMESTAMP
    };

    % collections (a la derecha de datasets)
    \node[tblh, right=2.6cm of Hds] (Hcol) {collections};
    \node[tbl, below=-0.4pt of Hcol.south, anchor=north] (Bcol) {%
      \underline{global\_id} : SERIAL \\
      \textit{dataset\_id} : INT FK \\
      original\_id : INT \\
      name : VARCHAR
    };

    % entities (debajo de collections)
    \node[tblh, below=2.1cm of Bcol] (Hent) {entities};
    \node[tbl, below=-0.4pt of Hent.south, anchor=north] (Bent) {%
      \underline{global\_id} : SERIAL \\
      \textit{collection\_global\_id} : INT FK \\
      original\_id : INT \\
      name : VARCHAR \\
      contents : JSONB
    };

    % metadata (a la derecha de entities)
    \node[tblh, right=2.6cm of Hent] (Hmd) {metadata};
    \node[tbl, below=-0.4pt of Hmd.south, anchor=north] (Bmd) {%
      \underline{id} : SERIAL \\
      \textit{entity\_id} : INT FK \\
      key : VARCHAR \\
      value : VARCHAR
    };

    % reference (debajo de entities)
    \node[tblh, below=2.1cm of Bent] (Href) {reference};
    \node[tbl, below=-0.4pt of Href.south, anchor=north] (Bref) {%
      \underline{id} : SERIAL \\
      \textit{source\_id} : INT FK \\
      \textit{target\_id} : INT FK \\
      reason : VARCHAR
    };

    % Flechas FK (N..1)
    \draw[fk] (Bcol.west) -- node[above,sloped,font=\footnotesize\rmfamily]{N..1} (Bds.east);
    \draw[fk] (Hent.north) -- node[right,font=\footnotesize\rmfamily]{N..1} (Bcol.south);
    \draw[fk] (Bmd.west) -- node[above,font=\footnotesize\rmfamily]{N..1} (Bent.east |- Bmd.west);
    \draw[fk] (Href.north) to[bend left=22]
        node[left,pos=0.5,font=\footnotesize\rmfamily]{source N..1} (Bent.south);
    \draw[fk] (Href.north) to[bend right=22]
        node[right,pos=0.5,font=\footnotesize\rmfamily]{target N..1} (Bent.south);
  \end{tikzpicture}}
  \caption{Modelo relacional implementado en PostgreSQL.}
  \label{fig:relationalModel}
\end{figure}

Más allá del paso mecánico de entidades a tablas, el esquema incorpora
tres decisiones de diseño que conviene comentar de forma explícita, ya
que responden a requisitos concretos del sistema y no a la traducción
E-R por defecto.

\textbf{Clave dual \texttt{global\_id} / \texttt{original\_id}.}
Tanto \texttt{collections} como \texttt{entities} mantienen dos
identificadores. El primero, \texttt{global\_id}, es una clave primaria
autogenerada que garantiza la unicidad global dentro de la base de
datos y se utiliza de manera uniforme como clave foránea desde el resto
del esquema. El segundo, \texttt{original\_id}, preserva el
identificador heredado de la fuente externa y mantiene la trazabilidad
con esa fuente, lo que permite reconciliar la información ante
reimportaciones del mismo \textit{dataset} sin romper las referencias internas.
Las restricciones de unicidad
(\texttt{UQ(dataset\_id, original\_id)} en \texttt{collections} y
\texttt{UQ(collection\_global\_id, original\_id)} en \texttt{entities})
imponen que el identificador original sea único \emph{dentro} de su
contexto, dejando que distintos datasets reutilicen los mismos
identificadores numéricos sin colisión.

\textbf{Columna \texttt{contents} de tipo JSONB.}
Cada entidad almacena, junto a sus metadatos normalizados, una copia
del documento original en formato JSONB. Esta redundancia controlada
cumple dos funciones complementarias. Por un lado, permite a la vista
de detalle acceder a campos no indexados, como sinopsis, URLs de
imagen u otros atributos del dominio que no se utilizan como facetas
exploratorias, sin acoplar el esquema a cada particularidad de cada
fuente. Por otro, mantiene desacoplada la ingesta respecto al esquema
relacional: si una fuente nueva expone campos adicionales, basta con
incluirlos en \texttt{contents} sin necesidad de migrar la base de
datos.

\textbf{Índices orientados a la consulta exploratoria.}
Los índices definidos sobre \texttt{metadata(key, value)},
\texttt{reference(source\_id, reason)} y
\texttt{reference(target\_id, reason)} no son casuales. Reflejan
directamente la forma de las subconsultas \texttt{EXISTS} que el
\texttt{NavigationDAO} genera al traducir el estado del usuario a SQL,
mecanismo descrito en detalle en la Sección~\ref{sec:filtrado}.
Indexar \texttt{metadata} por la pareja \texttt{(key, value)} permite
resolver eficientemente filtros del tipo ``género = Acción''. Indexar
la tabla \texttt{reference} por las dos columnas más selectivas en
cada dirección, \texttt{source\_id, reason} para el filtrado
relacional directo y \texttt{target\_id, reason} para la navegación
inversa, cubre los dos sentidos en que el sistema atraviesa las
relaciones entre colecciones.

\section{Representación del contexto de exploración}
\label{sec:representacionContexto}

Para comprender las decisiones de diseño del modelo de representación interno, es útil partir de un ejemplo
concreto: la estructura del \textit{dataset} de The Movie Database (TMDB), que fue la primera y principal fuente de datos
utilizada durante el desarrollo. TMDB organiza su información en torno a cinco tipos
de entidades: películas, series de televisión, personas, productoras y cadenas de televisión.
Cada tipo de entidad posee atributos propios --- como el género o la fecha de estreno en el caso
de las películas, o el género y la fecha de nacimiento --- y mantiene
relaciones directas con entidades de otros tipos. Así, una película puede estar producida por
una o varias productoras, contar con actores y miembros de equipo técnico de la colección de
personas, una serie puede estar asociada a una cadena de televisión concreta, etc. O sea, al final tenemos una 
base de datos suficientemente compleja, con atributos específicos al dominio de cada entidad, y posibles relaciones de 
navegación, con aristas que conectan entidades de igual o distinta naturaleza.

Estas relaciones son bidireccionales en el \textit{dataset} procesado: si una película referencia a una
persona como \textit{Director}, esa persona referencia a su vez a la película bajo la razón
\textit{Director}. Esto permite recorrer el grafo de relaciones en ambas direcciones desde
cualquier punto de partida.

Al analizar esta estructura se infirieron las dos abstracciones fundamentales del modelo
interno. Por un lado, los \textbf{metadatos}: atributos propios de cada entidad, representados
como pares clave-valor, sobre los que el usuario puede establecer condiciones de inclusión o
exclusión. Por otro lado, las \textbf{referencias}: vínculos entre entidades de distintas
colecciones, etiquetados con un tipo de relación (\textit{reason}), que permiten tanto filtrar
por asociación como navegar de una colección a otra siguiendo esos vínculos. Esta distinción
--- metadatos para describir, referencias para relacionar --- es la que articula toda la
representación del contexto de exploración.

Estas dos abstracciones, metadatos y referencias, se materializan en
código a través de tres clases que dan forma al modelo en memoria. La
Figura~\ref{fig:umlClases} las resume y muestra cómo se relacionan
entre sí, sirviendo de mapa antes de entrar en el detalle de cada una.
La clase \texttt{State} encapsula los filtros activos y el historial
de transiciones de una sesión; \texttt{StateManager} coordina el
estado activo, la pila de historial y los contextos guardados para
unión; \texttt{Link}, por último, captura una transición concreta
entre colecciones.

\begin{figure}[H]
  \centering

  \begin{tikzpicture}[
      umlname/.style={
        draw, rectangle, fill=blue!25, font=\footnotesize\bfseries,
        align=center, inner sep=4pt
      },
      umlbody/.style={
        draw, rectangle, fill=blue!5, font=\footnotesize\ttfamily,
        align=left, inner sep=4pt
      },
      compose/.style={thick, {Diamond[length=3.5mm,width=2.5mm]}-},
      aggreg/.style={thick, {Diamond[open,length=3.5mm,width=2.5mm]}-},
      assoc/.style={thick, -Stealth},
      lbl/.style={font=\footnotesize\rmfamily, fill=white, inner sep=2pt}
    ]
    % State (izquierda)
    \node[umlname, text width=5.5cm] (StateN) at (0,0) {State};
    \node[umlbody, text width=5.5cm, below=-0.4pt of StateN.south, anchor=north] (StateA) {%
      - datasetId : int \\
      - currentCollectionId : int \\
      - mfilters : Map\string<Str, Set\string<Str\string>{}\string> \\
      - notMfilters : Map\string<Str, Set\string<Str\string>{}\string> \\
      - rfilters : Map\string<Int, Map\string<Str, Set\string<Int\string>{}\string>{}\string> \\
      - notRfilters : Map\string<Int, Map\string<Str, Set\string<Int\string>{}\string>{}\string> \\
      - links : List\string<Link\string>
    };
    \node[umlbody, text width=5.5cm, below=-0.4pt of StateA.south, anchor=north] (StateM) {%
      + addMetadataFilter(String, String) : void \\
      + removeMetadataFilter(String, String) : void \\
      + addReferenceFilter(int, String, int) : void \\
      + removeReferenceFilter(int, String, int) : void \\
      + link(int) : void \\
      + goback(int, State) : void
    };


    % StateManager (derecha)
    \node[umlname, text width=5.5cm, right=4.5cm of StateN] (SMN) {StateManager};
    \node[umlbody, text width=5.5cm, below=-0.4pt of SMN.south, anchor=north] (SMA) {%
      - cur : State \\
      - historyStack : Deque\string<State\string> \\
      - unionSet : List\string<State\string> \\
      - linkStack : Deque\string<Link\string>
    };
    \node[umlbody, text width=5.5cm, below=-0.4pt of SMA.south, anchor=north] (SMM) {%
      + save() : void \\
      + restore() : void \\
      + link(int, String) : void \\
      + goback() : void \\
      + union(int) : void
    };


    % Link (abajo, centrado entre State y StateManager)
    \node[umlname, text width=5.5cm, anchor=north] (LN)
      at ($(StateM.south)!0.5!(SMM.south) + (0.65,-5)$) {Link};
    \node[umlbody, text width=5.5cm, below=-0.4pt of LN.south, anchor=north] (LA) {%
      - forward : boolean \\
      - collectionId : int \\
      - reason : String \\
      - state : State
    };

    % Relaciones
    % 1 cur: composición StateManager -> State
    \draw[compose] (SMN.west) -- node[lbl,above]{1\ cur} (StateN.east);
    % historyStack, unionSet: agregación horizontal (misma altura que StateA)
    \draw[aggreg] (SMA.west) -- node[lbl,above]{*\ historyStack, unionSet}
      (SMA.west -| StateA.east);
    % links: State contiene Links
    \draw[aggreg] (StateM.south east) -- ++(0,-0.5) -| node[lbl,pos=0.75,right]{*\ links} (LN.north west);
    % linkStack: StateManager contiene Links
    \draw[aggreg] (SMM.south) -- ++(0,-0.5) -| node[lbl,pos=0.75,below]{*\ linkStack} (LN.north east);
    % Link tiene un State (composición, como cur)
    \draw[compose] (LA.west) -- +(-0.6,0) |- node[lbl,pos=0.25,left]{1\ state} (StateM.south west);
  \end{tikzpicture}
  \caption[Diagrama UML del modelo de exploración]{Diagrama UML de clases del modelo de exploración en memoria.
  \texttt{State} en el momento de la transición.}
  \label{fig:umlClases}
\end{figure}

    \subsection{La clase \texttt{State}: filtros del tramo e historial de transiciones}

La clase \texttt{State} representa el contexto de exploración de un usuario en un momento dado.
Encapsula, por un lado, los filtros activos sobre la colección actual y, por otro, el historial
de transiciones realizadas entre colecciones durante la sesión actual.

Los filtros del tramo en curso se organizan en cuatro estructuras \texttt{HashMap}. Los filtros de
metadatos (\texttt{mfilters}) asocian cada atributo a un conjunto de valores seleccionados,
expresando que las entidades resultantes deben satisfacer al menos uno de dichos valores por
atributo. Su contraparte negativa (\texttt{notMfilters}) excluye las entidades que coincidan con los
valores indicados. Los filtros de referencia (\texttt{rfilters}) operan sobre relaciones entre
colecciones: para cada pareja de colección referenciada y tipo de relación, almacenan el conjunto de
identificadores de entidades con los que debe existir vínculo. De igual modo, \texttt{notRfilters}
excluye las entidades que presenten dichos vínculos. Estos cuatro mapas describen únicamente el
tramo en curso: cuando el usuario salta a otra colección mediante un \texttt{link}, los filtros del
tramo previo se preservan en el \textit{snapshot} capturado dentro del enlace, y el estado activo arranca
limpio sobre la nueva colección.

Junto a los filtros, \texttt{State} mantiene una lista de objetos \texttt{Link}. Cada
\texttt{Link} registra una transición entre colecciones: almacena la dirección de la transición
(bidireccional), la colección de origen, el tipo de relación seguida y una copia
del estado en el momento de realizar la transición. Esta lista constituye el historial de
navegación y es lo que permite reconstruir, a la hora de ejecutar una consulta, el camino
completo que el usuario ha recorrido entre colecciones.

\texttt{State} implementa \texttt{Serializable} y facilita un constructor de copia que realiza
una copia profunda de todas sus estructuras internas, necesaria para preservar instantáneas
del estado en distintos puntos de la sesión.

A modo de ejemplo concreto, la Figura~\ref{fig:stateExample} muestra
cómo se ve un \texttt{State} tras una secuencia típica de operaciones:
el usuario parte de la colección \textit{Movies} en el \textit{dataset} TMDB,
añade un filtro de metadatos \textit{Genres = Action} y a continuación
navega hacia \textit{People} siguiendo la relación \textit{Director}.
La colección activa ha pasado a \textit{People} y los cuatro mapas de filtros del estado activo
quedan vacíos, mientras que la entrada añadida a \texttt{links} registra la transición y guarda como
\textit{snapshot} el estado previo al salto, donde sobreviven los filtros aplicados sobre \textit{Movies}.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      every node/.style={font=\footnotesize},
      outer/.style={draw, rounded corners, fill=blue!5,
                    inner sep=8pt, align=left},
      header/.style={font=\footnotesize\bfseries},
      field/.style={draw, rounded corners=2pt, fill=white,
                    inner sep=4pt, align=left, font=\footnotesize\ttfamily},
      snap/.style={draw, rounded corners=2pt, fill=yellow!15,
                   inner sep=3pt, align=left, font=\scriptsize\ttfamily}
    ]
    \node[outer, text width=12cm] (state) {%
      \begin{tabular}{@{}l@{}}
        \textbf{State} \\[2pt]
        \ttfamily datasetId = 1 \rmfamily (TMDB) \\
        \ttfamily currentCollectionId = 3 \rmfamily (People) \\[4pt]
      \end{tabular}
      \\[-4pt]
      \tikz[baseline]{\node[field, text width=11cm]{%
        \textbf{mfilters} / \textbf{notMfilters} / \textbf{rfilters} / \textbf{notRfilters}: \{\ \} (vacíos)%
      };} \\[3pt]
      \tikz[baseline]{\node[field, text width=11cm]{%
        \textbf{links}: \\[2pt]
        \quad [0] Link(forward = true, collectionId = 1 (Movies), \\
        \quad\phantom{[0] }reason = ``Director'', \\
        \quad\phantom{[0] }state = \tikz[baseline]{\node[snap]{%
          \textit{snapshot}: currentCollectionId = 1, \\
          \phantom{snapshot:\ }mfilters = \{ ``Genres'' $\to$ \{``Action''\} \}, \\
          \phantom{snapshot:\ }links = [\ ]%
        };})%
      };}%
    };
  \end{tikzpicture}
  \caption[Ejemplo de objeto \texttt{State}]{Instantánea de un
  \texttt{State} tras filtrar películas por \textit{Genres = Action}
  y navegar a \textit{People} por la relación \textit{Director}. El estado
  activo ya describe \textit{People} con los cuatro mapas de filtros vacíos;
  los filtros aplicados sobre \textit{Movies} y la transición que cruzó al
  usuario quedan registrados en el \textit{snapshot} capturado dentro de \texttt{links}.}
  \label{fig:stateExample}
\end{figure}

    \subsection{\texttt{StateManager}: gestión del ciclo de vida del estado}

\texttt{StateManager} es el punto de entrada a través del cual los controladores interactúan
con el estado de navegación. Se instancia al inicio de la sesión, cuando el usuario selecciona
un \textit{dataset} y una colección de partida, y se almacena como atributo de la sesión HTTP bajo la
clave \texttt{navState}.

Internamente, \texttt{StateManager} coordina tres estructuras con ciclos de vida distintos.
La primera es el estado activo (\texttt{cur}), que representa el contexto de exploración en
curso y sobre el que se aplican todas las operaciones de filtrado y navegación. La segunda es
una pila de historial (\texttt{historyStack}), una \texttt{Deque} que el
\texttt{NavigationServlet} alimenta invocando \texttt{save()} antes de cada acción, lo que
permite revertir el estado mediante \texttt{restore()}. La tercera es el \texttt{unionSet},
una lista de estados congelados que acumula los contextos que el usuario ha marcado para
combinar mediante unión; al invocar \texttt{union()}, el estado activo se añade al
\texttt{unionSet} y \texttt{cur} se reinicializa con un estado limpio para continuar la
exploración de manera independiente.

A estas tres estructuras se suma una pila auxiliar, \texttt{linkStack}, que registra
los \texttt{Link} hacia delante creados por \texttt{link()} y que \texttt{goback()}
consume en orden inverso para saber qué transición revertir. A diferencia de las
anteriores, \texttt{linkStack} no representa contexto de exploración por sí misma: el
historial efectivo, el que el \texttt{NavigationDAO} traduce a SQL, vive dentro de cada
\texttt{State} en su campo \texttt{links}.

Es responsabilidad íntegra de \texttt{StateManager} la gestión de coherencia entre estas tres estructuras. 
Por ejemplo, al ejecutar \texttt{link()} o \texttt{union()}, es
\texttt{StateManager} quien decide cuándo realizar una copia profunda del estado activo
antes de cambiarlo, garantizando que los estados almacenados en el historial y en el
\texttt{unionSet} no se vean afectados por operaciones posteriores. La API pública
de \texttt{StateManager} refleja el vocabulario de acciones disponibles desde el
\textit{frontend} --- \texttt{addMetadataFilter}, \texttt{removeReferenceFilter}, \texttt{link},
\texttt{goback}, \texttt{save}, \texttt{restore} y \texttt{union} --- delegando en
\texttt{State} las operaciones sobre filtros, y aplicando directamente la lógica de
coordinación cuando la operación afecta a más de una de sus estructuras internas.

    \subsection{API REST: \textit{endpoints} expuestos al cliente}
    \label{subsec:endpoints}

El modelo de exploración descrito hasta aquí permanecería inerte sin
un mecanismo que lo expusiese al exterior. Esa función la cumple una
API HTTP basada en \textit{servlets} anotados con \texttt{@WebServlet},
en la que cada \textit{servlet} se ocupa de un recurso bien delimitado
y delega la lógica en \texttt{StateManager} o en \texttt{NavigationDAO}
según corresponda.

La traducción del modelo a esta API sigue una asimetría intencionada
entre escrituras y lecturas. Todas las acciones que modifican el
estado de exploración se canalizan a través de un único
\textit{endpoint}, \texttt{/api/navigation}, que recibe en el cuerpo
de la petición un campo \texttt{action} cuyo valor discrimina entre
las operaciones disponibles: añadir y retirar filtros de metadatos y
de referencia, navegar por un enlace, deshacer una transición,
restaurar el estado previo a una unión, abrir un nuevo bloque de
unión o cambiar de colección. De este modo, el vocabulario público
de \texttt{StateManager} se proyecta directamente sobre la capa
HTTP del \textit{backend}, sin necesidad de multiplicar \textit{endpoints} por
cada operación. Las consultas, por el contrario, sí justifican
\textit{endpoints} independientes, ya que cada una devuelve una
representación distinta del estado actual: las entidades visibles,
el detalle de una entidad concreta, las facetas de metadatos, las
facetas de referencia o los enlaces disponibles.

La correspondencia entre cada URL y la clase \texttt{*Servlet} que la
atiende es directa: \texttt{DatasetsServlet} resuelve
\texttt{/api/datasets}, \texttt{MetadataFacetsServlet} resuelve
\texttt{/api/facets/metadata}, y así sucesivamente. Todas las
peticiones que requieren contexto de exploración recuperan el
\texttt{StateManager} asociado a la sesión mediante
\texttt{req.getSession()} y devuelven \texttt{HTTP 400} cuando no
existe un estado activo, lo que garantiza que ningún \textit{endpoint}
opere sobre un cliente que aún no se ha inicializado.

El conjunto completo de \textit{endpoints} expuestos por el \textit{backend},
junto con sus parámetros principales y la acción que desencadenan, se
resume en la Tabla~\ref{tab:endpoints}.

\begin{table}[H]
  \centering
  \footnotesize
  \renewcommand{\arraystretch}{1.2}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{|l|p{4.1cm}|p{2.8cm}|p{4.7cm}|}
    \hline
    \textbf{Método} & \textbf{URL} & \textbf{Parámetros} & \textbf{Acción} \\
    \hline
    GET  & \texttt{/api/datasets}    & ---                       & Lista los \textit{datasets} disponibles \\
    \hline
    GET  & \texttt{/api/collections} & \texttt{datasetId}        & Lista las colecciones de un \textit{dataset} \\
    \hline
    GET  & \texttt{/api/session}     & ---                       & Devuelve el estado de la sesión activa \\
    \hline
    POST & \texttt{/api/session}     & \texttt{datasetId}, \texttt{collectionId} (cuerpo JSON) & Inicializa una sesión sobre un \textit{dataset} y una colección \\
    \hline
    GET  & \texttt{/api/entities}    & \texttt{page}, \texttt{size} & Entidades visibles tras aplicar los filtros activos (paginado) \\
    \hline
    GET  & \texttt{/api/entity}      & \texttt{id}, \texttt{collectionId} (opcional) & Detalle de una entidad: metadatos, recursos y referencias \\
    \hline
    GET  & \texttt{/api/facets/metadata}   & ---                 & Facetas de metadatos y filtros activos \\
    \hline
    GET  & \texttt{/api/facets/references} & ---                 & Facetas de referencia y filtros activos \\
    \hline
    GET  & \texttt{/api/facets/links}      & ---                 & Relaciones disponibles desde la colección actual \\
    \hline
    POST & \texttt{/api/navigation}  & \texttt{action} + parámetros propios de la acción & Aplica una acción sobre el estado: filtros (\texttt{add\_mfilter}, \texttt{rm\_mfilter}, \texttt{add\_not\_mfilter}, \texttt{rm\_not\_mfilter}, \texttt{add\_rfilter}, \texttt{rm\_rfilter}, \texttt{add\_not\_rfilter}, \texttt{rm\_not\_rfilter}), navegación (\texttt{link}, \texttt{goback}), unión (\texttt{union}, \texttt{change}) y restauración (\texttt{restore}) \\
    \hline
    GET  & \texttt{/api/union}       & \texttt{page}, \texttt{size} & Entidades del conjunto de unión (paginado) \\
    \hline
    GET  & \texttt{/api/resource/*}  & ruta como \texttt{pathInfo} & Sirve archivos del directorio de \textit{uploads} (imágenes y recursos asociados a entidades) \\
    \hline
  \end{tabular}
  \caption{Endpoints REST expuestos por el \textit{backend}.}
  \label{tab:endpoints}
\end{table}

\section{Filtrado clásico y relacional}
\label{sec:filtrado}

En TMDB, la exploración de un \textit{dataset} raramente se agota filtrando por atributos propios de
una colección, debido al tamaño y complejidad de la colección. Un caso de uso habitual es, por ejemplo,
encontrar películas de un género concreto dirigidas por una persona específica, o excluir series producidas por una
determinada compañía. El primer tipo de condición opera sobre los metadatos de la entidad; el segundo,
sobre sus relaciones con entidades de otra colección. Esta distinción entre \textit{filtrado
clásico} --- sobre atributos --- y \textit{filtrado relacional} --- sobre vínculos --- motivó el
diseño de dos mecanismos de filtrado independientes y de naturaleza distinta, pero que se aplican de forma
conjunta en cada consulta.

Ambos mecanismos admiten además una variante por la negativa: es posible no solo incluir entidades
que cumplan alguna condición, sino también excluir explícitamente las que la cumplan. La combinación de
inclusiones y exclusiones, tanto sobre metadatos como sobre referencias, define el espacio de
resultados que el usuario está explorando en cada momento.

    \subsection{Construcción dinámica de \textit{queries} SQL}

El núcleo de la capa de acceso a datos es la traducción del estado de navegación a una \textit{query}
SQL ejecutable. Esta traducción se ensambla de manera incremental: en cada petición, el
\texttt{NavigationDAO} construye la \textit{query} de forma incremental añadiendo cláusulas
\texttt{AND} sobre una consulta base que selecciona entidades de la colección activa.

El \texttt{appendFilterClauses} es el método central de este proceso, que recibe el  
\texttt{StringBuilder} de la \textit{query} en construcción, la lista de parámetros y el estado
actual, y le añade todas las condiciones derivadas de los filtros acumulados. Para el caso
más simple --- sin historial de navegación entre colecciones --- delega directamente en
\texttt{appendStateFilters}, que itera sobre los cuatro mapas de filtros del estado
(\texttt{mfilters}, \texttt{notMfilters}, \texttt{rfilters}, \texttt{notRfilters}) y
genera una subconsulta \texttt{EXISTS} o \texttt{NOT EXISTS} por cada condición activa.
Cuando el estado contiene además un historial de \textit{links}, \texttt{appendFilterClauses}
actúa de forma recursiva, generando subconsultas anidadas que incorporan el contexto de
cada transición previa; este mecanismo se describe en detalle en la
sección~\ref{sec:navegacion_transversal}.

Todas las \textit{queries} se construyen con parámetros posicionales (\texttt{?}), nunca
interpolando valores directamente en el SQL. Los valores se acumulan en una lista
\texttt{params} y se asignan posteriormente mediante \texttt{stmt.setObject()}, lo que
delega en el \texttt{PreparedStatement} de JDBC la responsabilidad de escapar los valores
antes de enviarlos al motor, para prevenir inyecciones SQL. El coste de filtrado se traslada
íntegramente a PostgreSQL, sin materializar subconjuntos intermedios de resultados en
memoria.

    \subsection{Filtros de metadatos}

Los filtros de metadatos permiten restringir los resultados de una colección en función de los
atributos clave-valor asociados a sus entidades. En el contexto de TMDB, por ejemplo, esto equivale a
operaciones como seleccionar únicamente películas del género \textit{Action}, o excluir
aquellas con idioma original distinto del inglés.

Cada filtro de inclusión se traduce en una subconsulta \texttt{EXISTS} sobre la tabla
\texttt{metadata}. Si el usuario ha seleccionado múltiples valores para el mismo atributo,
se genera una cláusula \texttt{EXISTS} independiente por cada valor, lo que impone que la
entidad deba satisfacer \textit{todos} ellos simultáneamente. Los filtros de exclusión
(\texttt{notMfilters}) siguen la misma estructura pero con \texttt{NOT EXISTS}, como
muestra el Código~\ref{lst:filtrosMetadata}.

\begin{lstlisting}[style=javaStyle, caption=Filtros de metadatos, label=lst:filtrosMetadata]
for (var entry : state.getMfilters().entrySet()) {
    String attr = entry.getKey();
    for (String val : entry.getValue()) {
        sql.append(" AND EXISTS (SELECT 1 FROM metadata " + m
            + " WHERE " + m + ".entity_id = " + e + ".global_id"
            + " AND " + m + ".key = ? AND " + m + ".value = ?) ");
        params.add(attr);
        params.add(val);
    }
}
\end{lstlisting}

Esta elección implica una semántica de conjunción estricta entre valores del mismo atributo,
lo cual puede parecer contraintuitivo cuando los valores son mutuamente excluyentes
--- como ocurre con \texttt{Genres} en TMDB, donde una película puede pertenecer a varios
géneros a la vez. En ese caso, seleccionar \textit{Action} y \textit{Drama} devuelve
las películas que tienen \textit{ambos} géneros, no las que tienen alguno de los dos.
Esta semántica se mantuvo de forma deliberada para garantizar consistencia con el
comportamiento del resto de filtros del sistema, y la describiremos como parte de los
obstáculos encontrados en la sección~\ref{sec:obstaculos}.

    \subsection{Filtros de referencia}

Los filtros de referencia permiten restringir los resultados de una colección en función
de sus vínculos con entidades concretas de otra colección. En TMDB, esto cubre casos como
mostrar únicamente las películas en las que una persona determinada participó como
\textit{Director}, o excluir películas con un \textit{Actor} específico.

Cada filtro de referencia está identificado por tres elementos: la colección referenciada,
el tipo de relación (\textit{reason}) y el identificador de la entidad concreta. Al igual
que con los metadatos, se genera una subconsulta \texttt{EXISTS} por cada combinación
activa, realizando un \texttt{JOIN} entre la tabla \texttt{reference}, la tabla
\texttt{entities} y la tabla \texttt{collections} para verificar la existencia de dicho
vínculo, tal y como recoge el Código~\ref{lst:filtrosReferencia}.

\begin{lstlisting}[style=javaStyle, caption=Filtros de referencia, label=lst:filtrosReferencia]
sql.append(" AND EXISTS ("
    + "SELECT 1 FROM reference " + r
    + " JOIN entities " + re + " ON " + r + ".target_id = " + re + ".global_id"
    + " JOIN collections " + rc + " ON " + re + ".collection_global_id = "
    + rc + ".global_id"
    + " WHERE " + r + ".source_id = " + e + ".global_id"
    + " AND " + rc + ".original_id = ?"
    + " AND " + rc + ".dataset_id = ?"
    + " AND " + r + ".reason = ?"
    + " AND " + re + ".original_id = ?) ");
\end{lstlisting}

Los filtros de exclusión (\texttt{notRfilters}) utilizan \texttt{NOT EXISTS} con la misma
estructura. Todos los filtros activos --- de metadatos y de referencia, en sus variantes
positiva o negativa --- se aplican conjuntamente sobre la misma consulta base, de forma
que el resultado final es la intersección de todas las condiciones establecidas por el
usuario en ese momento.

    \subsection{Facetas: cómputo dinámico de valores disponibles}

Las facetas son el mecanismo por el que el sistema informa al usuario de qué valores
siguen siendo relevantes dado el contexto de filtrado actual: qué géneros tienen al
menos una película entre los resultados visibles, qué directores aparecen en esas
películas, o a qué cadenas están asociadas las series que quedan tras aplicar los filtros.
Este cómputo es siempre dinámico: se recalcula en cada petición a partir del conjunto
de entidades que satisfacen el estado actual.

El sistema distingue dos tipos de facetas, ambas servidas por \texttt{FacetsServlet}
a través del \textit{endpoint} \texttt{GET /api/facets}:

Las \textbf{facetas de metadatos} se obtienen mediante \texttt{getFacets()}, que construye
una subconsulta con los identificadores de las entidades actualmente visibles
--- aplicando todos los filtros del estado --- y sobre ella ejecuta un \texttt{GROUP BY}
sobre la tabla \texttt{metadata}, según se aprecia en el
Código~\ref{lst:computoFacetas}.

\begin{lstlisting}[style=javaStyle, caption=Cómputo dinámico de facetas, label=lst:computoFacetas]
String sql =
    "SELECT m.key, m.value, COUNT(*) as cnt "
    + "FROM metadata m "
    + "WHERE m.entity_id IN (" + subquery + ") "
    + "GROUP BY m.key, m.value "
    + "ORDER BY m.key, cnt DESC";
\end{lstlisting}

El resultado de la \textit{query} se recorre fila a fila y se agrupa en un \texttt{LinkedHashMap}
indexado por atributo (\texttt{key}). Por cada fila, se construye un mapa con tres
campos: el valor del atributo (\texttt{value}), su frecuencia entre las entidades
visibles (\texttt{count}), y un booleano \texttt{active} que indica si ese valor ya
está seleccionado como filtro en el estado actual, que lo consulta mediante
\texttt{isFilterActive()}. El uso de \texttt{LinkedHashMap} preserva el orden de
inserción, que viene determinado por el \texttt{ORDER BY m.key, cnt DESC} de la \textit{query}:
los atributos aparecen en orden alfabético y, dentro de cada atributo, los valores se
listan de mayor a menor frecuencia. Este orden es el que el \textit{frontend} recibe y renderiza
directamente, sin necesidad de reordenar en el cliente.

Las \textbf{facetas de referencia} se obtienen mediante \texttt{getReferenceFacets()},
que sobre la misma subconsulta de entidades visibles realiza un \texttt{JOIN} con la
tabla \texttt{reference} para descubrir qué entidades de otras colecciones están
vinculadas a los resultados actuales, agrupadas por colección y tipo de relación.
El resultado expone, por ejemplo, qué personas concretas aparecen como \textit{Director}
entre las películas visibles, junto con el número de películas en las que aparecen.

Junto a las facetas, el mismo \textit{endpoint} devuelve los \textit{links} disponibles
--- obtenidos mediante \texttt{getAvailableLinks()} --- que indican a qué otras
colecciones es posible navegar desde el conjunto actual, y los filtros activos en ese
momento (\texttt{activeFilters}), permitiendo al \textit{frontend} reflejar el estado de
selección sin necesidad de mantener dicha información en el cliente.

\section{Navegación transversal}
\label{sec:navegacion_transversal}

Hasta ahora, hemos descrito los mecanismos de filtrado que operan dentro de una única colección:
dadas las entidades de \textit{Movies}, se restringe el conjunto según atributos propios
o según vínculos con entidades concretas de otras colecciones. Sin embargo, el sistema
permite también \textit{trasladarse} de una colección a otra siguiendo una relación,
preservando en el historial los filtros del tramo previo de modo que la consulta resultante
los integre por composición. Este mecanismo es lo que denominamos \textit{navegación transversal}.

Damos un ejemplo concreto en TMDB: el usuario está explorando películas con el filtro de género
\textit{Action} activo. Desde ese contexto, decide navegar hacia la colección
\textit{People} siguiendo la relación \textit{Actor}, para ver qué actores aparecen en
esas películas de acción. Tras el salto, la colección activa pasa a ser \textit{People},
pero el sistema recuerda que solo interesan las personas vinculadas al subconjunto
de películas que cumplían el filtro establecido anteriormente. El historial de transiciones compone, por tanto,
parte del contexto de exploración con responsabilidad expresa, no un simple registro de navegación.

    \subsection{El mecanismo de \texttt{link} y \texttt{goback}}

La transición entre colecciones se realiza mediante la operación \texttt{link}, que recibe
el identificador de la colección destino y el tipo de relación a seguir. Su implementación
en \texttt{StateManager}, reproducida en el Código~\ref{lst:link}, tiene cuatro efectos.

\begin{lstlisting}[style=javaStyle, caption=Operación \texttt{link}, label=lst:link]
public void link(int targetCollectionId, String reason) {
    Link newLink = new Link(true, this.cur.getCurrentCollectionId(),
                            reason, new State(this.cur));
    this.cur.link(targetCollectionId);
    this.cur.getLinks().add(newLink);
    this.linkStack.push(newLink);
}
\end{lstlisting}

Primero, se construye un nuevo objeto \texttt{Link} marcado como transición hacia delante
(\texttt{forward = true}). Este objeto almacena la colección de origen, el tipo de
relación seguida y una copia profunda del estado activo en el momento de la transición; los
filtros del tramo previo viven a partir de ese instante únicamente en el \textit{snapshot} del enlace,
sin contaminar el tramo siguiente. Segundo, se invoca \texttt{cur.link(targetCollectionId)}, que
desplaza la colección activa en \texttt{cur} a la colección destino y vacía los cuatro mapas de
filtros del estado activo, de modo que el nuevo tramo arranca limpio. Tercero, el nuevo
\texttt{Link} se añade a la lista de transiciones del propio \texttt{State}
(\texttt{cur.getLinks()}), que es la que el \texttt{NavigationDAO} consume al construir las
\textit{queries}. Cuarto, ese mismo \texttt{Link} se apila en \texttt{linkStack}, la pila auxiliar que
mantiene \texttt{StateManager} para conocer cuál es la última transición revertible en una
eventual operación de \texttt{goback}.

La Figura~\ref{fig:seqLink} resume la secuencia de mensajes entre el
cliente, el \texttt{NavigationServlet} y el modelo en memoria al
ejecutar una operación de \textit{link}. Se observa que el servlet
invoca primero \texttt{save()} (volcando una copia del estado actual a
\texttt{historyStack}) y a continuación \texttt{link()}, dentro de
la cual \texttt{StateManager} construye el \textit{snapshot}, desplaza la
colección activa de \texttt{cur} vaciando sus filtros y registra el
enlace tanto en \texttt{cur.getLinks()} como en \texttt{linkStack}.

\begin{figure}[H]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{sequencediagram}
    \newthread{c}{:Cliente}
    \newinst[2]{nav}{:NavigationServlet}
    \newinst[2]{sm}{:StateManager}
    \newinst[2]{state}{cur:State}

    \begin{call}{c}{POST /api/navigation (action=link)}{nav}{HTTP 200 JSON}
      \begin{call}{nav}{save()}{sm}{}
        \begin{call}{sm}{historyStack.push(new State(cur))}{sm}{}
        \end{call}
      \end{call}
      \begin{call}{nav}{link(collectionId, reason)}{sm}{}
        \begin{call}{sm}{new Link(true, curColl, reason, new State(cur))}{sm}{}
        \end{call}
        \begin{call}{sm}{link(collectionId)}{state}{}
        \end{call}
        \begin{call}{sm}{getLinks().add(newLink)}{state}{}
        \end{call}
        \begin{call}{sm}{linkStack.push(newLink)}{sm}{}
        \end{call}
      \end{call}
    \end{call}
  \end{sequencediagram}}
  \caption[Diagrama de secuencia de \texttt{link}]{Secuencia de
  mensajes durante la operación \texttt{link}. \texttt{save()} guarda
  una copia profunda del estado en \texttt{historyStack}; a
  continuación \texttt{link()} crea el nuevo \texttt{Link} con
  instantánea del estado previo, cambia la colección activa de
  \texttt{cur} vaciando sus filtros, y apila el enlace tanto en
  \texttt{cur.getLinks()} como en \texttt{linkStack}.}
  \label{fig:seqLink}
\end{figure}

La operación \texttt{goback} revierte la última transición hacia delante realizada en
la sesión. Su implementación se apoya en \texttt{linkStack}: extrae de la pila el último
\texttt{Link} apilado por \texttt{link()}, construye un nuevo \texttt{Link} con dirección
hacia atrás (\texttt{forward = false}) que registra esa reversión en la lista de
transiciones de \texttt{cur}, y restaura sobre \texttt{cur} tanto la colección de origen
como los cuatro mapas de filtros guardados en el \textit{snapshot} del \texttt{Link} extraído, de
modo que el tramo recuperado vuelve a ser exactamente el que el usuario había abandonado.
El Código~\ref{lst:goback} reproduce el método completo.

\begin{lstlisting}[style=javaStyle, caption=Operación \texttt{goback}, label=lst:goback]
public void goback() {
    Link last = this.linkStack.pop();
    Link backLink = new Link(false, this.cur.getCurrentCollectionId(),
                             last.getReason(), new State(this.cur));
    this.cur.goback(last.getCollectionId(), last.getState());
    this.cur.getLinks().add(backLink);
}
\end{lstlisting}

El hecho de que \texttt{goback} registre un nuevo \texttt{Link} en
\texttt{cur.getLinks()} en lugar de eliminar el último tiene una consecuencia relevante:
ese historial es siempre creciente y nunca se borra. Esto significa que la \textit{query} SQL
resultante debe recorrer toda la cadena de transiciones acumuladas, incluidos los
retrocesos, para reconstruir fielmente el contexto de exploración. La pila
\texttt{linkStack}, en cambio, sí pierde la entrada con cada \texttt{goback}, pero su rol
es estrictamente operacional: indicar cuál es la próxima reversión legal y nada más.

Esta asimetría queda reflejada en la
Figura~\ref{fig:seqGoback}: \texttt{linkStack.pop()} consume la cabeza
de la pila, mientras que el nuevo \texttt{Link} de retroceso se añade
únicamente a \texttt{cur.getLinks()}, dejando la cadena efectiva del
historial monótonamente creciente.

\begin{figure}[H]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{sequencediagram}
    \newthread{c}{:Cliente}
    \newinst[2]{nav}{:NavigationServlet}
    \newinst[2]{sm}{:StateManager}
    \newinst[2]{state}{cur:State}

    \begin{call}{c}{POST /api/navigation (action=goback)}{nav}{HTTP 200 JSON}
      \begin{call}{nav}{save()}{sm}{}
        \begin{call}{sm}{historyStack.push(new State(cur))}{sm}{}
        \end{call}
      \end{call}
      \begin{call}{nav}{goback()}{sm}{}
        \begin{call}{sm}{last = linkStack.pop()}{sm}{}
        \end{call}
        \begin{call}{sm}{new Link(false, curColl, last.reason, new State(cur))}{sm}{}
        \end{call}
        \begin{call}{sm}{goback(last.collectionId, last.state)}{state}{}
        \end{call}
        \begin{call}{sm}{getLinks().add(backLink)}{state}{}
        \end{call}
      \end{call}
    \end{call}
  \end{sequencediagram}}
  \caption[Diagrama de secuencia de \texttt{goback}]{Secuencia de
  mensajes durante la operación \texttt{goback}. \texttt{linkStack}
  sí se reduce con \texttt{pop()}, pero el historial efectivo en
  \texttt{cur.getLinks()} crece con un nuevo \texttt{Link} marcado
  como retroceso (\texttt{forward = false}), preservando toda la
  cadena de transiciones para reconstruir el contexto en SQL.}
  \label{fig:seqGoback}
\end{figure}

    \subsection{Subconsultas anidadas: traducción del historial de \textit{links} a SQL}

Cuando el estado contiene un historial de transiciones, \texttt{appendFilterClauses}
no puede limitarse a aplicar los filtros del estado activo: debe también expresar en SQL
la restricción que impone cada transición puesta anteriormente. Para ello utiliza una
sobrecarga recursiva que recorre la lista de transiciones del estado
(\texttt{state.getLinks()}) desde el último \texttt{Link} hacia el primero, generando en
cada nivel una subconsulta anidada.

La estructura de la recursión tiene la siguiente definición: para cada nivel de profundidad, se aplican
primero los filtros del estado en ese nivel mediante \texttt{appendStateFilters}, y a
continuación se genera una cláusula \texttt{AND ... IN (SELECT ...)} que restringe las
entidades del nivel actual a aquellas que, a través de la relación registrada en el
\texttt{Link}, están vinculadas a entidades del nivel siguiente. La dirección del
\texttt{Link} determina qué columna de la tabla \texttt{reference} actúa como origen y
cuál como destino, como se observa en el Código~\ref{lst:subconsultasAnidadas}.

\begin{lstlisting}[style=javaStyle, caption=Subconsultas anidadas, label=lst:subconsultasAnidadas]
if (link.isForward()) {
    sql.append(" AND " + e + ".global_id IN ("
        + "SELECT " + r_link + ".target_id FROM reference " + r_link
        + " JOIN entities " + next_e + " ON " + r_link + ".source_id = "
        + next_e + ".global_id"
        + " JOIN collections " + next_c + " ON " + next_e
        + ".collection_global_id = " + next_c + ".global_id"
        + " WHERE " + next_c + ".dataset_id = ? AND " + next_c
        + ".original_id = ? AND " + r_link + ".reason = ? ");
} else {
    sql.append(" AND " + e + ".global_id IN ("
        + "SELECT " + r_link + ".source_id FROM reference " + r_link
        + " JOIN entities " + next_e + " ON " + r_link + ".target_id = "
        + next_e + ".global_id"
        + " JOIN collections " + next_c + " ON " + next_e
        + ".collection_global_id = " + next_c + ".global_id"
        + " WHERE " + next_c + ".dataset_id = ? AND " + next_c
        + ".original_id = ? AND " + r_link + ".reason = ? ");
}
appendFilterClauses(sql, params, link.getState(), linkList, index - 1, depth + 1);
sql.append(") ");
\end{lstlisting}

Cada llamada recursiva abre una subconsulta que se cierra después de que la llamada
interna haya completado su propio nivel, resultando en una estructura de paréntesis
anidados que refleja correctamente la cadena de transiciones del usuario. Los identificadores de tabla
(\texttt{e}, \texttt{e1}, \texttt{e2}, \texttt{rl0}, \texttt{rl1}\ldots) se generan
dinámicamente a partir del parámetro \texttt{depth} para evitar colisiones entre niveles.

Retomando el ejemplo anterior --- películas de acción, navegación hacia \textit{People}
por la relación \textit{Actor} --- la \textit{query} resultante para obtener los actores visibles
adopta la forma mostrada en el Código~\ref{lst:queryResultante}.

\begin{lstlisting}[style=sqlStyle, caption={[\textit{Query} SQL de la navegación transversal]\textit{Query} SQL resultante de la navegación transversal}, label=lst:queryResultante]
SELECT DISTINCT e.original_id, e.name
FROM entities e
JOIN collections c ON e.collection_global_id = c.global_id
WHERE c.dataset_id = ? AND c.original_id = ?   -- People
AND e.global_id IN (
    SELECT rl0.source_id FROM reference rl0
    JOIN entities e1 ON rl0.target_id = e1.global_id
    JOIN collections c1 ON e1.collection_global_id = c1.global_id
    WHERE c1.dataset_id = ? AND c1.original_id = ?  -- Movies
    AND rl0.reason = 'Actor'
    AND EXISTS (SELECT 1 FROM metadata m0  -- Genres = Action
                WHERE m0.entity_id = e1.global_id
                AND m0.key = ? AND m0.value = ?)
)
\end{lstlisting}

La subconsulta interior opera sobre la colección \textit{Movies} con sus filtros activos,
y la consulta exterior recupera únicamente las personas de \textit{People} que aparecen
como origen de una relación \textit{Actor} hacia alguna de esas películas.

\section{Operaciones de conjuntos}
\label{sec:operaciones_conjuntos}

Los mecanismos de filtrado y navegación transversal descritos hasta ahora operan siempre
sobre un único contexto de exploración activo. Sin embargo, hay casos de uso que requieren
combinar los resultados de dos o más contextos construidos de manera independiente. Un posible 
ejemplo concreto en TMDB: el usuario quiere obtener todas las personas que han trabajado como
\textit{Director} en películas de acción o como \textit{Actor} en series de
ciencia ficción. No sabemos si hay intersección entre ambos conjuntos, y construir ambas
condiciones simultáneamente dentro de un único estado sería impracticable, y  posiblemente imposible con 
la semántica del filtrado descrito en la sección~\ref{sec:filtrado}.

Para cubrir este caso, el sistema incorpora una operación de unión, donde el usuario puede
guardar el contexto actual, iniciar una nueva exploración desde cero y, al consultar
los resultados, obtener las entidades que satisfacen cualquiera de los contextos
acumulados hasta entonces.

    \subsection{Construcción del \texttt{unionSet}}

La operación \texttt{union} en \texttt{StateManager} guarda el contexto actual para una
unión posterior y reinicializa el estado activo desde cero. El Código~\ref{lst:union}
reproduce su implementación, que delega la parte de reinicio en el método auxiliar
\texttt{change}.

\begin{lstlisting}[style=javaStyle, caption=Operación \texttt{union}, label=lst:union]
public void union(int collectionId) {
    this.unionSet.add(this.cur);
    change(collectionId);
}

public void change(int collectionId) {
    this.historyStack.clear();
    this.cur = new State(this.datasetId, collectionId);
}
\end{lstlisting}

Primero, se añade el estado activo actual al \texttt{unionSet} sin realizar ninguna
copia: \texttt{cur} va a ser reemplazado inmediatamente, por lo que no hay riesgo de
que modificaciones posteriores alteren la entrada guardada, que ya contiene su propio
historial de transiciones dentro de \texttt{cur.getLinks()}. A continuación se invoca
\texttt{change}, que vacía la \texttt{historyStack} y reinicializa \texttt{cur} con un
\texttt{State} limpio sobre la colección indicada. Ese nuevo estado arranca con su
propia lista de transiciones vacía, lo que desvincula por completo el contexto recién
iniciado del anterior.

El \texttt{unionSet} puede acumular un número arbitrario de entradas repitiendo esta
operación. Cada entrada es un estado completo e independiente, con su propia colección
activa, sus propios filtros y su propio historial de transiciones.

La Figura~\ref{fig:seqUnion} ilustra la secuencia de mensajes
correspondiente. A diferencia de \texttt{link} y \texttt{goback}, la
operación no toca \texttt{State} directamente: el estado anterior se
añade tal cual a \texttt{unionSet} (sin copia profunda, dado que
\texttt{cur} va a ser reemplazado), y \texttt{change()} instala un
\texttt{State} recién creado como nuevo contexto activo.

\begin{figure}[H]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{sequencediagram}
    \newthread{c}{:Cliente}
    \newinst[2]{nav}{:NavigationServlet}
    \newinst[2]{sm}{:StateManager}
    \newinst[2]{state}{:State}

    \begin{call}{c}{POST /api/navigation (action=union, collectionId)}{nav}{HTTP 200 JSON}
      \begin{call}{nav}{save()}{sm}{}
        \begin{call}{sm}{historyStack.push(new State(cur))}{sm}{}
        \end{call}
      \end{call}
      \begin{call}{nav}{union(collectionId)}{sm}{}
        \begin{call}{sm}{unionSet.add(cur)}{sm}{}
        \end{call}
        \begin{call}{sm}{change(collectionId)}{sm}{}
          \begin{call}{sm}{historyStack.clear()}{sm}{}
          \end{call}
          \begin{call}{sm}{new State(datasetId, collectionId)}{state}{cur}
          \end{call}
        \end{call}
      \end{call}
    \end{call}
  \end{sequencediagram}}
  \caption[Diagrama de secuencia de \texttt{union}]{Secuencia de
  mensajes durante la operación \texttt{union}. El estado activo
  pasa a \texttt{unionSet} sin copia profunda y \texttt{change()}
  vacía \texttt{historyStack} antes de reinicializar \texttt{cur} con
  un \texttt{State} limpio sobre la colección indicada. La pila
  \texttt{linkStack} no se modifica.}
  \label{fig:seqUnion}
\end{figure}

    \subsection{Resolución de la unión en base de datos}

La consulta del \texttt{unionSet} se realiza a través del \textit{endpoint}
\texttt{GET /api/union}, servido por \texttt{UnionServlet}, que delega en los métodos
\texttt{getUnionEntities()} y \texttt{countUnionEntities()} del \texttt{NavigationDAO}.

Ambos métodos construyen una única \textit{query} que itera sobre todas las entradas del
\texttt{unionSet} y concatena un bloque de condiciones por entrada, separados por
\texttt{OR}, como ilustra el Código~\ref{lst:queryUnion}.

\begin{lstlisting}[style=javaStyle, caption=\textit{Query} de unión, label=lst:queryUnion]
for (int i = 0; i < unionSet.size(); i++) {
    State state = unionSet.get(i);
    if (i > 0) sql.append(" OR ");
    sql.append(" (c.dataset_id = ? AND c.original_id = ? ");
    params.add(state.getDatasetId());
    params.add(state.getCurrentCollectionId());
    appendFilterClauses(sql, params, state);
    sql.append(" ) ");
}
\end{lstlisting}

Cada bloque es estructuralmente idéntico a la \textit{query} que se generaría para ese estado en
una consulta normal, aplicando \texttt{appendFilterClauses} sobre el estado correspondiente,
lo que incluye sus filtros de metadatos, sus filtros de referencia y, si procede, las
subconsultas anidadas derivadas de su historial de transiciones. La unión entre bloques
se expresa con \texttt{OR} a nivel de la cláusula \texttt{WHERE}, y el \texttt{DISTINCT}
en el \texttt{SELECT} garantiza que las entidades presentes en más de un bloque no
aparezcan duplicadas en los resultados.

Una consecuencia de este diseño es que la unión puede combinar entradas sobre
colecciones distintas. En el ejemplo anterior, el primer bloque opera sobre
\textit{People} filtrado por películas de acción con relación \textit{Director}, y el
segundo también sobre \textit{People} pero filtrado por series de ciencia ficción con
relación \textit{Actor}. Que ambas entradas compartan colección activa no es un requisito
del sistema: el campo \texttt{collection\_id} devuelto por \texttt{getUnionEntities()}
indica a qué colección pertenece cada entidad del resultado, permitiendo al \textit{frontend}
distinguirlas cuando fuera necesario.

\section{Desarrollo del \textit{frontend}}
\label{sec:frontend}

Concluida la descripción del \textit{backend}, queda por presentar la capa
que el usuario percibe directamente. El \textit{frontend} se concibió como una
capa de presentación deliberadamente delgada, cuya única
responsabilidad consiste en renderizar el estado actual del servidor
y en traducir las acciones del usuario en peticiones HTTP contra los
\textit{endpoints} descritos en la Tabla~\ref{tab:endpoints}. El
cliente no mantiene una copia del modelo de datos ni implementa una
lógica propia de navegación: el \texttt{StateManager} del servidor es
la única fuente de verdad, y la interfaz se limita a presentar lo que
recibe y a invocar el \textit{endpoint} adecuado ante cada acción del
usuario.

    \subsection*{Tecnología y filosofía}

Para una capa con esa responsabilidad acotada, recurrir a un
\textit{framework} reactivo como React, Vue o Angular habría supuesto
un sobrecoste injustificado. El cliente está implementado, por tanto,
en JavaScript moderno con módulos \textit{ES}, sin dependencias de
interfaz. Tres razones sostienen esta decisión. En primer lugar, su
lógica se reduce a peticiones HTTP y a manipulación directa del DOM,
sin estructuras de datos derivadas que justifiquen un sistema reactivo.
En segundo lugar, la gestión del estado vive íntegramente en el
servidor a través de \texttt{StateManager}, por lo que no es necesaria
una capa equivalente en el cliente. Por último, mantener el código en
JavaScript estándar facilita la lectura y la evolución del proyecto,
especialmente en un contexto académico en el que la base de código
debe poder revisarse sin requerir conocimientos específicos de un
\textit{framework} concreto.

La única dependencia de desarrollo es \textbf{Vite}, que actúa como
servidor de desarrollo durante la iteración, con recarga en caliente,
y como empaquetador de producción al generar el \textit{bundle}
estático mediante \texttt{npm run build}.

    \subsection*{Estructura}

Sobre esa base mínima de herramientas, el árbol de directorios del
cliente se organiza de forma plana y predecible:

\begin{itemize}
  \item \texttt{index.html}: punto de entrada HTML. Declara las
    regiones visuales (selección de \textit{dataset}, selección de
    colección y pantalla de navegación) y los contenedores donde el
    \textit{script} inyecta dinámicamente facetas, entidades y
    filtros activos.
  \item \texttt{src/main.js}: punto de entrada JavaScript. Orquesta
    la inicialización, mantiene un objeto \texttt{state} con la
    información mínima necesaria para renderizar (identificadores y
    nombres de la colección actual, historial de \textit{links} o
    página activa, entre otros) y registra los \textit{listeners}
    sobre los controles de la interfaz.
  \item \texttt{src/api.js}: cliente HTTP, descrito más adelante.
  \item \texttt{src/utils.js}: utilidades genéricas, como la
    selección de nodos por \texttt{id} o el escape de HTML.
  \item \texttt{src/components/}: piezas de interfaz reutilizables.
    Cada archivo encapsula un componente independiente, con una API
    mínima: combobox con búsqueda incremental para las facetas
    (\texttt{Combobox.js}), \textit{chips} que representan los filtros
    activos (\texttt{FilterTags.js}), ventana modal de detalle de
    entidad (\texttt{Modal.js}), control de paginación
    (\texttt{PaginationHandler.js}), reordenamiento de paneles por
    \textit{drag and drop} (\texttt{PanelDragOrder.js}),
    \textit{placeholders} de carga (\texttt{Skeleton.js}) y
    notificaciones efímeras (\texttt{Toast.js}).
  \item \texttt{src/styles/}: hojas de estilo
    (\texttt{base.css}, \texttt{layout.css} y \texttt{main.css}).
\end{itemize}

    \subsection*{Comunicación con el \textit{backend}}

A todas estas piezas y a la orquestación de \texttt{main.js} les da
soporte una capa transversal: el cliente HTTP centralizado en
\texttt{src/api.js}. Esta pieza expone una única función,
\texttt{api(endpoint, options)}, construida sobre \texttt{fetch}, que
fija \texttt{mode: \string'cors\string'}, \texttt{credentials: \string'include\string'} y la
cabecera \texttt{Content-Type: application/\-json}, serializa
automáticamente el cuerpo de las peticiones \texttt{POST} y lanza una
excepción si la respuesta no es satisfactoria. El uso de
\texttt{credentials: \string'include\string'} es lo que garantiza que la cookie
\texttt{JSESSIONID} viaje en cada petición, permitiendo al \textit{backend}
recuperar el \texttt{StateManager} asociado a la sesión.

Coherente con la decisión arquitectónica que sitúa el estado en el
servidor, el cliente no duplica esa información. Tras cada acción,
ya sea aplicar un filtro, navegar por un enlace, retroceder o añadir
al conjunto de unión, se invoca una función \texttt{refreshAll()} que
vuelve a llamar a los \textit{endpoints} de facetas, entidades y
unión, y repinta la interfaz con los datos recibidos. Esta estrategia,
alineada con el espíritu HATEOAS discutido en el
Capítulo~\ref{cap:estadoDeLaCuestion}, simplifica el razonamiento
sobre coherencia: el servidor es la única fuente de verdad, y la
interfaz refleja en todo momento el resultado que aquel devuelve.

\section{Ejemplo ilustrativo (recorrido end-to-end)}
\label{sec:ejemplo_recorrido}
\shorthandoff{"}
\sloppy

Las secciones anteriores han presentado por separado las piezas que
componen el motor de navegación: el \texttt{State} acumulado, las
transformaciones que aplica \texttt{StateManager}, la traducción a SQL
realizada por \texttt{NavigationDAO} y el flujo HTTP que conecta el
\textit{frontend} con todo lo anterior. La presente sección integra esas piezas
en un ejemplo ilustrativo, ejecutado sobre un \textit{dataset} deliberadamente
reducido. El objetivo es mostrar cómo cada acción primitiva del API
(\texttt{add\_mfilter}, \texttt{add\_not\_mfilter},
\texttt{add\_rfilter}, \texttt{link}, \texttt{goback} y \texttt{union})
modifica de manera observable el conjunto de entidades visibles, y
permitir al lector seguir paso a paso la evolución conjunta del estado
interno del sistema y de la consulta SQL que esa acción provoca.

Para que cada operación quede plenamente apreciada, el ejemplo se
organiza en tres flujos independientes. Cada flujo arranca desde el
estado inicial sobre el \textit{dataset} y demuestra una categoría distinta de
operaciones: el primero acota progresivamente un conjunto dentro de una
sola colección, el segundo viaja entre colecciones aplicando filtros en
destino que repercuten en el conjunto de origen, y el tercero combina
contextos construidos por separado sobre colecciones diferentes. Dentro
de cada flujo, todos los pasos exhiben la acción invocada por el usuario
y el conjunto que resulta, mientras que el \texttt{State} completo, la
consulta SQL y la captura de la interfaz se presentan al final como
síntesis del recorrido.

El \textit{dataset} que sustenta el ejemplo se distribuye junto al código fuente,
en el archivo \path{scripts/test/example_dataset.json}, siguiendo el
mismo formato (un objeto JSON con los campos \texttt{collections} y
\texttt{objects}) que el resto de \textit{datasets} de prueba presentes
en \path{scripts/test/}. Las capturas que acompañan a cada flujo se
obtuvieron ejecutando el sistema sobre ese \textit{dataset} y reproduciendo
manualmente la secuencia de acciones correspondiente.

\subsection{Mini-\textit{dataset} reproducible}
\label{subsec:ejemplo_dataset}

El \textit{dataset} agrupa tres colecciones cuyos tamaños se han elegido
deliberadamente asimétricos: cuatro entidades en \textit{Movies}, cuatro
en \textit{People} y dos en \textit{Studios}. La asimetría persigue que
los filtros y las navegaciones produzcan resultados informativos, ni
vacíos ni saturados, sobre conjuntos lo bastante pequeños como para que
el lector pueda comprobar mentalmente cada paso. La
Tabla~\ref{tab:ejemplo_datasets} y la Tabla~\ref{tab:ejemplo_collections}
reflejan, respectivamente, el \textit{dataset} y las colecciones que lo
componen, tal y como aparecen físicamente en la base de datos.

\begin{table}[h]
  \centering
  \begin{tabular}{|c|l|l|}
    \hline
    \textbf{id} & \textbf{name} & \textbf{created\_at} \\ \hline
    1 & tmdb-toy & 2025-01-01 \\ \hline
  \end{tabular}
  \caption{Tabla \texttt{datasets} del mini-\textit{dataset}.}
  \label{tab:ejemplo_datasets}
\end{table}

\begin{table}[h]
  \centering
  \begin{tabular}{|c|c|c|l|}
    \hline
    \textbf{global\_id} & \textbf{dataset\_id} & \textbf{original\_id} & \textbf{name} \\ \hline
    1 & 1 & 1 & Movies  \\ \hline
    2 & 1 & 2 & People  \\ \hline
    3 & 1 & 3 & Studios \\ \hline
  \end{tabular}
  \caption[Tabla \texttt{collections} del mini-\textit{dataset}]{Tabla \texttt{collections}: tres colecciones bajo el \textit{dataset} \texttt{tmdb-toy}.}
  \label{tab:ejemplo_collections}
\end{table}

\FloatBarrier

Las entidades concretas se reparten por colección en las
Tablas~\ref{tab:ejemplo_movies}, \ref{tab:ejemplo_people}
y~\ref{tab:ejemplo_studios}. Por brevedad se omite la columna
\texttt{contents}, cuyo único uso relevante en este recorrido es el
nombre de la entidad, ya reflejado en la columna \textbf{name}.

\begin{table}[h]
  \centering
  \begin{tabular}{|c|c|c|l|}
    \hline
    \textbf{global\_id} & \textbf{collection\_global\_id} & \textbf{original\_id} & \textbf{name} \\ \hline
    10 & 1 &  27205 & Inception              \\ \hline
    11 & 1 & 157336 & Interstellar           \\ \hline
    12 & 1 &  24428 & The Avengers           \\ \hline
    13 & 1 &  49026 & The Dark Knight Rises  \\ \hline
  \end{tabular}
  \caption[Tabla \texttt{entities} --- \textit{Movies}]{Tabla \texttt{entities} restringida a la colección \textit{Movies}.}
  \label{tab:ejemplo_movies}
\end{table}

\begin{table}[h]
  \centering
  \begin{tabular}{|c|c|c|l|}
    \hline
    \textbf{global\_id} & \textbf{collection\_global\_id} & \textbf{original\_id} & \textbf{name} \\ \hline
    20 & 2 &  525 & Christopher Nolan \\ \hline
    21 & 2 & 6193 & Leonardo DiCaprio \\ \hline
    22 & 2 & 3223 & Robert Downey Jr. \\ \hline
    23 & 2 & 3894 & Christian Bale    \\ \hline
  \end{tabular}
  \caption[Tabla \texttt{entities} --- \textit{People}]{Tabla \texttt{entities} restringida a la colección \textit{People}.}
  \label{tab:ejemplo_people}
\end{table}

\begin{table}[h]
  \centering
  \begin{tabular}{|c|c|c|l|}
    \hline
    \textbf{global\_id} & \textbf{collection\_global\_id} & \textbf{original\_id} & \textbf{name} \\ \hline
    30 & 3 & 174 & Warner Bros. Pictures \\ \hline
    31 & 3 & 420 & Marvel Studios        \\ \hline
  \end{tabular}
  \caption[Tabla \texttt{entities} --- \textit{Studios}]{Tabla \texttt{entities} restringida a la colección \textit{Studios}.}
  \label{tab:ejemplo_studios}
\end{table}

\FloatBarrier

A continuación, la Tabla~\ref{tab:ejemplo_metadata} recoge los metadatos
vinculados a cada entidad. Las películas se anotan con género
(\texttt{Genres}) y año (\texttt{Year}), las personas con su lugar de
nacimiento (\texttt{Birthplace}) y los estudios con su año de fundación
(\texttt{Founded}). Son precisamente estos pares clave/valor los que
darán soporte a los filtros \texttt{add\_mfilter} y
\texttt{add\_not\_mfilter} en las escenas posteriores.

\begin{table}[h]
  \centering
  \footnotesize
  \begin{tabular}{|c|c|l|l|}
    \hline
    \textbf{id} & \textbf{entity\_id} & \textbf{key} & \textbf{value} \\ \hline
    1  & 10 & Genres     & Action        \\ \hline
    2  & 10 & Genres     & Sci-Fi        \\ \hline
    3  & 10 & Year       & 2010          \\ \hline
    4  & 11 & Genres     & Sci-Fi        \\ \hline
    5  & 11 & Genres     & Drama         \\ \hline
    6  & 11 & Year       & 2014          \\ \hline
    7  & 12 & Genres     & Action        \\ \hline
    8  & 12 & Year       & 2012          \\ \hline
    9  & 13 & Genres     & Action        \\ \hline
    10 & 13 & Genres     & Drama         \\ \hline
    11 & 13 & Year       & 2012          \\ \hline
    12 & 20 & Birthplace & London        \\ \hline
    13 & 21 & Birthplace & Los Angeles   \\ \hline
    14 & 22 & Birthplace & New York      \\ \hline
    15 & 23 & Birthplace & Haverfordwest \\ \hline
    16 & 30 & Founded    & 1923          \\ \hline
    17 & 31 & Founded    & 1993          \\ \hline
  \end{tabular}
  \caption[Tabla \texttt{metadata} del mini-\textit{dataset}]{Tabla \texttt{metadata}: pares clave/valor por entidad.}
  \label{tab:ejemplo_metadata}
\end{table}

\FloatBarrier

Cierra el \textit{dataset} la Tabla~\ref{tab:ejemplo_reference}, que enumera las
aristas almacenadas en la tabla \texttt{reference} mediante las columnas
\textit{source\_id}, \textit{target\_id} y \textit{reason}. Estas filas
son las que sostienen tanto a \texttt{add\_rfilter} como a \texttt{link}.
Por las decisiones de diseño descritas en la
Sección~\ref{sec:modeloDatos}, cada relación se persiste de forma
explícita en sus dos sentidos. La arista \textit{Director} entre
\textit{Inception} y \textit{Christopher Nolan}, por ejemplo, aparece
tanto como $(10 \to 20)$ como $(20 \to 10)$, lo que permite al motor
recorrerla con la misma eficiencia en cualquier dirección de
navegación.

\begin{table}[h]
  \centering
  \footnotesize
  \begin{tabular}{|c|c|c|l|l|}
    \hline
    \textbf{id} & \textbf{source\_id} & \textbf{target\_id} & \textbf{reason} & \textbf{sentido} \\ \hline
    1  & 10 & 20 & Director   & Inception $\to$ Nolan    \\ \hline
    2  & 10 & 21 & Actor      & Inception $\to$ DiCaprio \\ \hline
    3  & 10 & 30 & Production & Inception $\to$ Warner   \\ \hline
    4  & 11 & 20 & Director   & Interstellar $\to$ Nolan  \\ \hline
    5  & 11 & 30 & Production & Interstellar $\to$ Warner \\ \hline
    6  & 12 & 22 & Actor      & Avengers $\to$ Downey     \\ \hline
    7  & 12 & 31 & Production & Avengers $\to$ Marvel     \\ \hline
    8  & 13 & 20 & Director   & TDKR $\to$ Nolan     \\ \hline
    9  & 13 & 23 & Actor      & TDKR $\to$ Bale      \\ \hline
    10 & 13 & 30 & Production & TDKR $\to$ Warner    \\ \hline
    11 & 20 & 10 & Director   & Nolan $\leftarrow$ Inception      \\ \hline
    12 & 20 & 11 & Director   & Nolan $\leftarrow$ Interstellar   \\ \hline
    13 & 20 & 13 & Director   & Nolan $\leftarrow$ TDKR           \\ \hline
    14 & 21 & 10 & Actor      & DiCaprio $\leftarrow$ Inception   \\ \hline
    15 & 22 & 12 & Actor      & Downey $\leftarrow$ Avengers      \\ \hline
    16 & 23 & 13 & Actor      & Bale $\leftarrow$ TDKR            \\ \hline
    17 & 30 & 10 & Production & Warner $\leftarrow$ Inception     \\ \hline
    18 & 30 & 11 & Production & Warner $\leftarrow$ Interstellar  \\ \hline
    19 & 30 & 13 & Production & Warner $\leftarrow$ TDKR          \\ \hline
    20 & 31 & 12 & Production & Marvel $\leftarrow$ Avengers      \\ \hline
  \end{tabular}
  \caption[Tabla \texttt{reference} del mini-\textit{dataset}]{Tabla \texttt{reference}: aristas dirigidas entre entidades. La
  columna \textit{sentido} es meramente explicativa.}
  \label{tab:ejemplo_reference}
\end{table}

\FloatBarrier

\subsection{Vista esquemática del \textit{dataset}}
\label{subsec:ejemplo_esquema}

Las tablas anteriores son indispensables para reproducir el \textit{dataset},
pero su forma tabular oculta la estructura relacional que subyace al
ejemplo. Para complementarlas, la Figura~\ref{fig:ejemplo_fichas} ofrece
una vista alternativa en la que cada entidad recibe una ficha propia con
sus metadatos y sus referencias salientes. Conviene tenerla a mano
durante el recorrido posterior, pues constituye un mapa visual de las
conexiones que las escenas van explotando una tras otra.

\begin{figure}[h]
  \centering
  \footnotesize

  {\bfseries\color{blue!60!black} Movies}\par\vspace{2pt}
  \fcolorbox{blue!60!black}{blue!5}{\begin{minipage}{0.44\linewidth}\ttfamily
  Inception\quad(id=10, oid=27205)\\
  $\bullet$ Genres: Action, Sci-Fi\\
  $\bullet$ Year: 2010\\
  $\to$ Director:   Christopher Nolan\\
  $\to$ Actor:      Leonardo DiCaprio\\
  $\to$ Production: Warner Bros.
  \end{minipage}}\hfill
  \fcolorbox{blue!60!black}{blue!5}{\begin{minipage}{0.44\linewidth}\ttfamily
  Interstellar\quad(id=11, oid=157336)\\
  $\bullet$ Genres: Sci-Fi, Drama\\
  $\bullet$ Year: 2014\\
  $\to$ Director:   Christopher Nolan\\
  $\to$ Production: Warner Bros.
  \end{minipage}}

  \vspace{3pt}

  \fcolorbox{blue!60!black}{blue!5}{\begin{minipage}{0.44\linewidth}\ttfamily
  The Avengers\quad(id=12, oid=24428)\\
  $\bullet$ Genres: Action\\
  $\bullet$ Year: 2012\\
  $\to$ Actor:      Robert Downey Jr.\\
  $\to$ Production: Marvel Studios
  \end{minipage}}\hfill
  \fcolorbox{blue!60!black}{blue!5}{\begin{minipage}{0.44\linewidth}\ttfamily
  TDKR\quad(id=13, oid=49026)\\
  $\bullet$ Genres: Action, Drama\\
  $\bullet$ Year: 2012\\
  $\to$ Director:   Christopher Nolan\\
  $\to$ Actor:      Christian Bale\\
  $\to$ Production: Warner Bros.
  \end{minipage}}

  \vspace{8pt}

  {\bfseries\color{green!45!black} People}\par\vspace{2pt}
  \fcolorbox{green!50!black}{green!7}{\begin{minipage}{0.44\linewidth}\ttfamily
  Christopher Nolan\quad(id=20, oid=525)\\
  $\bullet$ Birthplace: London\\
  $\leftarrow$ Director: Inception\\
  $\leftarrow$ Director: Interstellar\\
  $\leftarrow$ Director: TDKR
  \end{minipage}}\hfill
  \fcolorbox{green!50!black}{green!7}{\begin{minipage}{0.44\linewidth}\ttfamily
  Leonardo DiCaprio\quad(id=21, oid=6193)\\
  $\bullet$ Birthplace: Los Angeles\\
  $\leftarrow$ Actor: Inception
  \end{minipage}}

  \vspace{3pt}

  \fcolorbox{green!50!black}{green!7}{\begin{minipage}{0.44\linewidth}\ttfamily
  Robert Downey Jr.\quad(id=22, oid=3223)\\
  $\bullet$ Birthplace: New York\\
  $\leftarrow$ Actor: The Avengers
  \end{minipage}}\hfill
  \fcolorbox{green!50!black}{green!7}{\begin{minipage}{0.44\linewidth}\ttfamily
  Christian Bale\quad(id=23, oid=3894)\\
  $\bullet$ Birthplace: Haverfordwest\\
  $\leftarrow$ Actor: TDKR
  \end{minipage}}

  \vspace{8pt}

  {\bfseries\color{orange!80!black} Studios}\par\vspace{2pt}
  \fcolorbox{orange!80!black}{orange!10}{\begin{minipage}{0.44\linewidth}\ttfamily
  Warner Bros. Pictures\quad(id=30, oid=174)\\
  $\bullet$ Founded: 1923\\
  $\leftarrow$ Production: Inception\\
  $\leftarrow$ Production: Interstellar\\
  $\leftarrow$ Production: TDKR
  \end{minipage}}\hfill
  \fcolorbox{orange!80!black}{orange!10}{\begin{minipage}{0.44\linewidth}\ttfamily
  Marvel Studios\quad(id=31, oid=420)\\
  $\bullet$ Founded: 1993\\
  $\leftarrow$ Production: The Avengers
  \end{minipage}}
  \caption[Fichas por entidad del mini-\textit{dataset}]{Fichas por entidad del mini-\textit{dataset}, agrupadas por colección
  y coloreadas en consecuencia: \textit{Movies} en azul, \textit{People}
  en verde y \textit{Studios} en naranja. Los puntos resumen los
  metadatos; las flechas $\to$ marcan referencias salientes y
  $\leftarrow$ referencias entrantes.}
  \label{fig:ejemplo_fichas}
\end{figure}

\FloatBarrier

\subsection{Estado inicial}
\label{subsec:ejemplo_estado_inicial}

Cada uno de los tres flujos que siguen arranca desde el estado inicial
del sistema: el \textit{frontend} acaba de abrir la colección \textit{Movies}
sobre el \textit{dataset} descrito, no hay ningún filtro activo y la cuadrícula
central enumera las cuatro películas
(Figura~\ref{fig:ejemplo_estado_inicial}). Internamente, el
\texttt{StateManager} mantiene un único \texttt{State} sin
\texttt{links} ni \texttt{unionSet}, y todos los mapas de filtros
están vacíos. A partir de ese punto, cada flujo aplica
una secuencia distinta de acciones para ilustrar una categoría
diferente de operaciones.

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.9\textwidth]{Imagenes/Bitmap/ejemplo/flujo0_inicial.png}
  \caption[Estado inicial del \textit{frontend}]{Estado inicial del \textit{frontend}, antes de aplicar ninguna acción:
  colección \textit{Movies} abierta y las cuatro entidades visibles.}
  \label{fig:ejemplo_estado_inicial}
\end{figure}

\FloatBarrier

\subsection{Flujo 1: acotación dentro de una colección}
\label{subsec:flujo_acotacion}

El primer flujo demuestra los tres operadores de filtrado disponibles
sobre una misma colección, encadenados de modo que cada uno reduzca de
manera observable el conjunto de entidades visibles. Es la forma de
exploración más cercana al funcionamiento de un buscador clásico, y
cubre las acciones \texttt{add\_mfilter}, \texttt{add\_not\_mfilter} y
\texttt{add\_rfilter}.

\textbf{Paso 1.1.} Sobre las cuatro películas iniciales, el usuario
invoca \texttt{addMetadataFilter} con la clave \texttt{"Genres"} y el
valor \texttt{"Action"}. El conjunto se reduce a tres entidades:
$\{$\textit{Inception}, \textit{The Avengers}, \textit{The Dark Knight
Rises}$\}$. \textit{Interstellar} queda fuera, ya que sus únicos
géneros son \textit{Sci-Fi} y \textit{Drama}
(Figura~\ref{fig:flujo1_paso1}).

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo1_paso1.png}
  \caption[Flujo~1, Paso~1.1]{Flujo~1, Paso~1.1. Tras aplicar \textit{Genres = Action},
  el panel de facetas muestra un único chip activo y la cuadrícula
  enumera tres películas.}
  \label{fig:flujo1_paso1}
\end{figure}

\FloatBarrier

\textbf{Paso 1.2.} Para descartar también las películas dramáticas, el
usuario añade un filtro de exclusión:
\texttt{addNotMetadataFilter} con clave \texttt{"Genres"} y valor
\texttt{"Drama"}. \textit{The Dark Knight Rises}, que combina
\textit{Action} y \textit{Drama}, desaparece, dejando dos entidades
visibles: $\{$\textit{Inception}, \textit{The Avengers}$\}$
(Figura~\ref{fig:flujo1_paso2}).

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo1_paso2.png}
  \caption[Flujo~1, Paso~1.2]{Flujo~1, Paso~1.2. El panel de facetas combina un chip
  positivo (\textit{Genres = Action}) y otro negativo (\textit{Genres
  $\neq$ Drama}); en la cuadrícula quedan dos películas.}
  \label{fig:flujo1_paso2}
\end{figure}

\FloatBarrier

\textbf{Paso 1.3.} Finalmente, el usuario filtra por estudio productor
con \texttt{addReferenceFilter}, pasando como colección referenciada el
$3$ (\textit{Studios}), como motivo \texttt{"Production"} y como
identificador externo el $420$ (\textit{Marvel Studios}).
\textit{Inception}, producida por \textit{Warner Bros.}, queda
descartada. El resultado final es $\{$\textit{The Avengers}$\}$, una
única entidad de las cuatro iniciales
(Figura~\ref{fig:flujo1_paso3}).

\begin{figure}[h]
  \centering
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo1_paso3.png}
  \caption[Flujo~1, Paso~1.3]{Flujo~1, Paso~1.3. Tres chips activos en el panel de
  facetas y \textit{The Avengers} como único resultado en la
  cuadrícula central.}
  \label{fig:flujo1_paso3}
\end{figure}

\FloatBarrier

\textbf{State.} Tras los tres pasos, el \texttt{cur} acumula los tres
filtros sobre \textit{Movies} y permanece desligado de cualquier
historial de navegación (Figura~\ref{fig:flujo1_state}).

\begin{figure}[h]
  \centering
  \fbox{\begin{minipage}{0.92\linewidth}\small
  \texttt{cur.currentCollectionId} = Movies (1) \\
  \texttt{mfilters}    = \{ Genres $\to$ \{Action\} \} \\
  \texttt{notMfilters} = \{ Genres $\to$ \{Drama\} \} \\
  \texttt{rfilters}    = \{ 3 $\to$ \{ Production $\to$ \{420\} \} \}\quad \texttt{notRfilters} = \{\,\} \\
  \texttt{linkList}    = [\,]\quad \texttt{unionSet} = [\,]
  \end{minipage}}
  \caption[Estado final del Flujo~1]{Estado final del Flujo~1, con un filtro inclusivo, uno de
  exclusión y uno de referencia activos.}
  \label{fig:flujo1_state}
\end{figure}

\textbf{SQL.} Como la lista de \texttt{links} sigue vacía, la
sobrecarga simple de \texttt{appendFilterClauses} se limita a invocar a
\texttt{appendStateFilters} en el nivel exterior. La consulta resultante
combina un \texttt{EXISTS}, un \texttt{NOT EXISTS} y un segundo
\texttt{EXISTS} que cruza la tabla \texttt{reference}, tal como
recoge el Código~\ref{lst:flujo1}.

\begin{lstlisting}[style=sqlStyle, label=lst:flujo1, caption={[SQL del Flujo~1]SQL acumulada al final del Flujo~1.}]
SELECT DISTINCT e.original_id, e.name
FROM entities e
JOIN collections c ON e.collection_global_id = c.global_id
WHERE c.dataset_id = 1 AND c.original_id = 1
  AND EXISTS     (SELECT 1 FROM metadata m0
                  WHERE m0.entity_id = e.global_id
                    AND m0.key = 'Genres' AND m0.value = 'Action')
  AND NOT EXISTS (SELECT 1 FROM metadata m0
                  WHERE m0.entity_id = e.global_id
                    AND m0.key = 'Genres' AND m0.value = 'Drama')
  AND EXISTS     (SELECT 1 FROM reference r0
                  JOIN entities re0 ON r0.target_id = re0.global_id
                  JOIN collections rc0 ON re0.collection_global_id = rc0.global_id
                  WHERE r0.source_id = e.global_id
                    AND rc0.original_id = 3 AND rc0.dataset_id = 1
                    AND r0.reason = 'Production' AND re0.original_id = 420)
ORDER BY e.name LIMIT 40 OFFSET 0
\end{lstlisting}

\FloatBarrier

\subsection{Flujo 2: navegación entre colecciones}
\label{subsec:flujo_navegacion}

El segundo flujo parte de nuevo del estado inicial y muestra cómo se
encadenan \texttt{link} y \texttt{goback} alrededor de un filtro
intermedio para responder preguntas que ningún filtro local podría
plantear. Cada uno de los tres pasos admite una lectura semántica
clara: al saltar a \textit{People} estamos preguntando qué personas
son actores en alguna película visible, al filtrar dentro de
\textit{People} restringimos esa pregunta a las que además cumplen
una propiedad concreta, y al volver a \textit{Movies} obtenemos, en
sentido inverso, las películas cuyo reparto incluye precisamente a
esas personas.

\textbf{Paso 2.1.} Sobre las cuatro películas iniciales, y sin ningún
filtro activo sobre \textit{Movies}, el usuario sigue la relación
\textit{Actor} invocando \texttt{link} con destino $2$
(\textit{People}) y motivo \texttt{"Actor"}. Al partir de una
colección sin filtros, la pregunta que el sistema responde es la más
amplia posible en este sentido: ¿qué personas figuran como actoras de
alguna película del catálogo? El motor recorre las aristas
\textit{Actor} desde cada película hacia su reparto, y la vista pasa
de las cuatro películas iniciales a las tres personas que aparecen
como actores: $\{$\textit{Leonardo DiCaprio}, \textit{Robert Downey
Jr.}, \textit{Christian Bale}$\}$. \textit{Christopher Nolan}, que
sólo figura en el \textit{dataset} como director, queda lógicamente fuera
(Figura~\ref{fig:flujo2_paso1}). Internamente, el
\texttt{StateManager} apila un \texttt{Link} con \texttt{forward =
true}, conserva una copia del \texttt{State} previo sobre
\textit{Movies} y desplaza \texttt{cur.currentCollectionId} a
\textit{People}.

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo2_paso1.png}
  \caption[Flujo~2, Paso~2.1]{Flujo~2, Paso~2.1. El \textit{breadcrumb} indica
  \textit{Movies} $\to$ \textit{People (Actor)} y la cuadrícula
  contiene los tres actores presentes en alguna película.}
  \label{fig:flujo2_paso1}
\end{figure}

\FloatBarrier

\textbf{Paso 2.2.} Ya en \textit{People}, el usuario añade un filtro
sobre el nuevo \texttt{cur} mediante \texttt{addMetadataFilter} con
clave \texttt{"Birthplace"} y valor \texttt{"Los Angeles"}. La
pregunta del paso anterior queda refinada: ¿qué personas son actoras
de alguna película y, además, nacieron en Los Angeles? El conjunto se
restringe a $\{$\textit{Leonardo DiCaprio}$\}$, único actor del
\textit{dataset} con esa ciudad como lugar de nacimiento
(Figura~\ref{fig:flujo2_paso2}). Conviene observar que el filtro se
almacena en el estado activo mientras \textit{People} es la colección actual; en
el paso siguiente, \texttt{goback} vaciará los filtros del estado al regresar a
\textit{Movies}, pero su huella sobrevivirá dentro del \textit{snapshot} que ese mismo
\texttt{goback} registre como link de retroceso, y volverá a tener efecto en la
SQL del paso final.

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo2_paso2.png}
  \caption[Flujo~2, Paso~2.2]{Flujo~2, Paso~2.2. El filtro \textit{Birthplace = Los
  Angeles} aparece como chip activo en el panel y deja una sola
  persona en la cuadrícula.}
  \label{fig:flujo2_paso2}
\end{figure}

\FloatBarrier

\textbf{Paso 2.3.} Para regresar a \textit{Movies}, el usuario invoca
\texttt{goback()}. Como se detalló en la
Sección~\ref{sec:navegacion_transversal}, esta operación no elimina
el \texttt{Link} anterior, sino que apila un segundo \texttt{Link}
con \texttt{forward = false} sobre el primero. El \texttt{cur} vuelve
a \textit{Movies} y la pregunta se invierte respecto al paso 2.1:
ahora interrogamos qué películas tienen en su reparto a alguna de
esas personas filtradas, es decir, a algún actor nacido en Los
Angeles. La generación de SQL recorre la cadena
\textit{Movies} $\to$ \textit{People} $\to$ \textit{Movies}
aplicando, en el tramo intermedio, el filtro \textit{Birthplace} preservado en
el \textit{snapshot} del \texttt{Link} de retroceso, y devuelve las películas cuyo
reparto incluye a alguna de esas personas. El resultado es una única película,
$\{$\textit{Inception}$\}$, en la que actúa \textit{DiCaprio}
(Figura~\ref{fig:flujo2_paso3}). Sin el filtro intermedio sobre
\textit{People}, el viaje de ida y vuelta habría devuelto las cuatro
películas originales, esto es, todas las que cuentan con algún actor
en el \textit{dataset}.

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo2_paso3.png}
  \caption[Flujo~2, Paso~2.3]{Flujo~2, Paso~2.3. El \textit{breadcrumb} extendido
  (\textit{Movies} $\to$ \textit{People (Actor)} $\to$
  \textit{Movies (Actor)}) e \textit{Inception} como único resultado
  reflejan la cadena completa de transiciones.}
  \label{fig:flujo2_paso3}
\end{figure}

\FloatBarrier

\textbf{State.} El \texttt{linkList} contiene ahora dos entradas, una
hacia adelante y otra de retroceso (Figura~\ref{fig:flujo2_state}). El
filtro de \textit{Birthplace}, aunque ya no aparezca activo en el
\texttt{cur} actual (que está sobre \textit{Movies}), vive dentro de la
copia profunda del \texttt{State} sobre \textit{People} guardada en
$\mathbf{L}_1$, y por eso interviene en la SQL del retroceso.

\begin{figure}[h]
  \centering
  \fbox{\begin{minipage}{0.95\linewidth}\small
  \texttt{cur.currentCollectionId} = Movies (1) \\
  \texttt{mfilters}    = \{\,\}\quad \texttt{notMfilters} = \{\,\}\quad \texttt{rfilters} = \{\,\}\quad \texttt{notRfilters} = \{\,\} \\
  \texttt{linkList} = [\, $\mathbf{L}_0$,\; $\mathbf{L}_1$ \,] \\
  \quad$\mathbf{L}_0$ = \texttt{Link(forward=true,  from=Movies, reason="Actor", state=$\langle$Movies sin filtros$\rangle$)} \\
  \quad$\mathbf{L}_1$ = \texttt{Link(forward=false, from=People, reason="Actor", state=$\langle$People + Birthplace=LA$\rangle$)} \\
  \texttt{unionSet} = [\,]
  \end{minipage}}
  \caption[Estado final del Flujo~2]{Estado final del Flujo~2. El filtro aplicado en
  \textit{People} reside dentro del \texttt{state} de $\mathbf{L}_1$ y
  participa en la SQL recursiva del retroceso.}
  \label{fig:flujo2_state}
\end{figure}

\textbf{SQL.} La sobrecarga recursiva de \texttt{appendFilterClauses}
recorre el \texttt{linkList} de fuera hacia adentro y emite dos niveles
de subconsulta anidada. La rama \texttt{backward} de $\mathbf{L}_1$
usa \texttt{rl0.source\_id} y, en su interior, materializa el filtro de
\textit{Birthplace} aplicado durante el Paso~2.2. La rama interna
corresponde al \texttt{forward} original de $\mathbf{L}_0$, sin filtros
sobre \textit{Movies} (Código~\ref{lst:flujo2}).

\begin{lstlisting}[style=sqlStyle, label=lst:flujo2, caption={[SQL del Flujo~2]SQL acumulada al final del Flujo~2: el filtro \textit{Birthplace = Los Angeles} aplicado en \textit{People} restringe el conjunto visible de \textit{Movies}.}]
SELECT DISTINCT e.original_id, e.name
FROM entities e
JOIN collections c ON e.collection_global_id = c.global_id
WHERE c.dataset_id = 1 AND c.original_id = 1
  AND e.global_id IN (
    SELECT rl0.source_id FROM reference rl0
    JOIN entities e1 ON rl0.target_id = e1.global_id
    JOIN collections c1 ON e1.collection_global_id = c1.global_id
    WHERE c1.dataset_id = 1 AND c1.original_id = 2
      AND rl0.reason = 'Actor'
      AND EXISTS (SELECT 1 FROM metadata m1
                  WHERE m1.entity_id = e1.global_id
                    AND m1.key = 'Birthplace' AND m1.value = 'Los Angeles')
      AND e1.global_id IN (
        SELECT rl1.target_id FROM reference rl1
        JOIN entities e2 ON rl1.source_id = e2.global_id
        JOIN collections c2 ON e2.collection_global_id = c2.global_id
        WHERE c2.dataset_id = 1 AND c2.original_id = 1
          AND rl1.reason = 'Actor'
      )
  )
ORDER BY e.name LIMIT 40 OFFSET 0
\end{lstlisting}

El recorrido completo de las dos transiciones, junto con el resultado
final de una sola película, queda reflejado en la
Figura~\ref{fig:flujo2_paso3}. La diferencia con el inicio del flujo
es notable: se ha pasado de cuatro entidades a una mediante un filtro
aplicado en otra colección.

\FloatBarrier

\subsection{Flujo 3: unión de contextos independientes}
\label{subsec:flujo_union}

El tercer flujo parte una vez más del estado inicial y explora la
última operación del API. La acción \texttt{union} permite guardar un
contexto construido sobre una colección, comenzar otro independiente
sobre otra colección distinta y combinar ambos en una sola respuesta.

\textbf{Paso 3.1.} Sobre las cuatro películas iniciales, el usuario
aplica \texttt{addMetadataFilter} con clave \texttt{"Genres"} y valor
\texttt{"Sci-Fi"}. El conjunto visible se reduce a
$\{$\textit{Inception}, \textit{Interstellar}$\}$
(Figura~\ref{fig:flujo3_paso1}).

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo3_paso1.png}
  \caption[Flujo~3, Paso~3.1]{Flujo~3, Paso~3.1. \textit{Genres = Sci-Fi} como chip
  activo y dos películas en la cuadrícula.}
  \label{fig:flujo3_paso1}
\end{figure}

\FloatBarrier

\textbf{Paso 3.2.} A continuación, el usuario invoca \texttt{union(2)}
para guardar el contexto recién construido y abrir uno nuevo sobre
\textit{People}. La operación traslada el \texttt{cur} actual al
\texttt{unionSet} y lo sustituye por un \texttt{State} en blanco sobre
\textit{People}. La cuadrícula pasa a mostrar las cuatro personas del
\textit{dataset}, ya que el nuevo contexto no tiene aún filtros activos, mientras
que el panel de unión refleja por primera vez el contexto guardado
(Figura~\ref{fig:flujo3_paso2}).

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo3_paso2.png}
  \caption[Flujo~3, Paso~3.2]{Flujo~3, Paso~3.2. La interfaz expone simultáneamente el
  panel de unión con el contexto recién guardado
  (\textit{Movies + Sci-Fi}) y la cuadrícula sobre el nuevo \texttt{cur}
  vacío en \textit{People}.}
  \label{fig:flujo3_paso2}
\end{figure}

\FloatBarrier

\textbf{Paso 3.3.} Sobre \textit{People}, el usuario aplica
\texttt{addMetadataFilter} con clave \texttt{"Birthplace"} y valor
\texttt{"Haverfordwest"}. El conjunto se restringe a
$\{$\textit{Christian Bale}$\}$
(Figura~\ref{fig:flujo3_paso3}).

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo3_paso3.png}
  \caption[Flujo~3, Paso~3.3]{Flujo~3, Paso~3.3. El chip \textit{Birthplace = Haverfordwest}
  reduce a una sola persona la cuadrícula de \textit{People},
  mientras que el panel de unión sigue conteniendo el contexto
  guardado en el paso anterior.}
  \label{fig:flujo3_paso3}
\end{figure}

\FloatBarrier

\textbf{Paso 3.4.} Por último, el usuario invoca de nuevo
\texttt{union(1)} para añadir también este segundo contexto al
\texttt{unionSet} y comenzar uno nuevo sobre \textit{Movies}. Tras este
paso, el \texttt{unionSet} contiene dos entradas heterogéneas: un
\texttt{State} sobre \textit{Movies} con el filtro \textit{Sci-Fi}, y
otro sobre \textit{People} con el filtro \textit{Birthplace = Haverfordwest}
(Figura~\ref{fig:flujo3_paso4}).

\begin{figure}[h]
  \centering
  % TODO: incluir captura definitiva
  \includegraphics[width=0.85\textwidth]{Imagenes/Bitmap/ejemplo/flujo3_paso4.png}
  \caption[Flujo~3, Paso~3.4]{Flujo~3, Paso~3.4. La vista de unión combina los tres
  resultados procedentes de las dos colecciones (dos películas en
  azul y una persona en verde), siguiendo el código cromático
  introducido en la Figura~\ref{fig:ejemplo_fichas}.}
  \label{fig:flujo3_paso4}
\end{figure}

\FloatBarrier

\textbf{State.} La Figura~\ref{fig:flujo3_state} muestra el contenido
del \texttt{unionSet} al final del flujo. El nuevo \texttt{cur},
limpio sobre \textit{Movies}, no influye en la respuesta del \textit{endpoint}
\texttt{/api/union}, que opera exclusivamente sobre las entradas del
\texttt{unionSet}.

\begin{figure}[h]
  \centering
  \fbox{\begin{minipage}{0.95\linewidth}\small
  \texttt{unionSet} = [ \\
  \quad$\langle$ Movies (1), \texttt{mfilters} = \{ Genres $\to$ \{Sci-Fi\} \} $\rangle$, \\
  \quad$\langle$ People (2), \texttt{mfilters} = \{ Birthplace $\to$ \{Haverfordwest\} \} $\rangle$ \\
  ] \\
  \texttt{cur.currentCollectionId} = Movies (1) \\
  \texttt{mfilters} = \{\,\}\quad \texttt{notMfilters} = \{\,\}\quad \texttt{rfilters} = \{\,\}\quad \texttt{notRfilters} = \{\,\} \\
  \texttt{linkList} = [\,]
  \end{minipage}}
  \caption[Estado final del Flujo~3]{Estado final del Flujo~3. El \texttt{unionSet} retiene los
  dos contextos sobre colecciones distintas; el \texttt{cur} actual
  está vacío.}
  \label{fig:flujo3_state}
\end{figure}

\textbf{SQL.} Al solicitar \texttt{GET /api/union}, el método
\texttt{getUnionEntities(unionSet, page, size)} construye un único
\texttt{SELECT} con un bloque \texttt{WHERE} por entrada del
\texttt{unionSet}, separados por \texttt{OR}. Cada bloque vuelve a
invocar \texttt{appendFilterClauses} con su propio \texttt{State}, de
modo que las dos colecciones del \texttt{unionSet} dan lugar a dos
ramas independientes en la condición (Código~\ref{lst:flujo3}). La
proyección añade \texttt{c.original\_id AS collection\_id} para que el
\textit{frontend} pueda distinguir el origen de cada entidad en la respuesta
combinada.

\begin{lstlisting}[style=sqlStyle, label=lst:flujo3, caption={[SQL del Flujo~3]SQL emitida por \texttt{getUnionEntities} con dos contextos de colecciones distintas en el \texttt{unionSet}.}]
SELECT DISTINCT e.original_id, e.name, c.original_id AS collection_id
FROM entities e
JOIN collections c ON e.collection_global_id = c.global_id
WHERE
  ( c.dataset_id = 1 AND c.original_id = 1
    AND EXISTS (SELECT 1 FROM metadata m0
                WHERE m0.entity_id = e.global_id
                  AND m0.key = 'Genres' AND m0.value = 'Sci-Fi')
  )
  OR
  ( c.dataset_id = 1 AND c.original_id = 2
    AND EXISTS (SELECT 1 FROM metadata m0
                WHERE m0.entity_id = e.global_id
                  AND m0.key = 'Birthplace' AND m0.value = 'Haverfordwest')
  )
ORDER BY e.name LIMIT 40 OFFSET 0
\end{lstlisting}

\textbf{Resultado.} El bloque~1 aporta $\{$\textit{Inception},
\textit{Interstellar}$\}$. El bloque~2 aporta $\{$\textit{Christian Bale}$\}$. 
La respuesta combina las tres entidades en una única lista,
con su \texttt{collection\_id} explícito en cada fila para que la
interfaz pueda renderizarlas según su tipo. 

\FloatBarrier
\fussy
\shorthandon{"}

\section{Obstáculos encontrados y soluciones implementadas}
\label{sec:obstaculos}

A lo largo del desarrollo surgieron obstáculos de naturaleza diversa: algunos conceptuales,
relacionados con la definición del modelo de datos y la lógica de navegación; otros técnicos,
derivados de las particularidades del entorno de ejecución. Esta sección recoge los más
relevantes, describiendo el problema encontrado y la solución adoptada en cada caso.

    \subsection*{Confusión conceptual en el prototipo inicial}

El primer obstáculo fue de carácter conceptual y afectó al punto de partida del desarrollo.
El modelo navegacional que articula el sistema --- colecciones, referencias, filtros
acumulados, transiciones --- no tiene un equivalente directo en los sistemas de exploración
de datos más habituales, lo que dificultó establecer desde el principio una representación
interna clara.

La historia de ese aprendizaje y las lecciones del prototipo previo se relatan ya en la
sección~\ref{subsec:prototipoModeloFormal} del capítulo de arquitectura, donde además se
formaliza el modelo definitivo. A los efectos de este capítulo basta con dejar constancia
del coste técnico que tuvo aquella confusión: una deuda temprana que fue necesario
resolver antes de poder implementar mecanismos más complejos como la navegación
transversal o las operaciones de unión, y que motivó la separación estricta de
responsabilidades entre modelo (\texttt{State}, \texttt{StateManager}), capa de acceso a
datos (\texttt{NavigationDAO}) y controladores que se describe a lo largo de este
capítulo.

    \subsection*{Tamaño de las colecciones y trabajo con subconjuntos}

TMDB expone colecciones de tamaño considerable: solo en películas y series, el catálogo
completo supera con holgura las decenas de miles de entidades. Trabajar con el \textit{dataset}
completo durante el desarrollo habría introducido tiempos de carga, poblado de base de
datos y ejecución de \textit{queries} que habrían dificultado la iteración rápida.

La solución adoptada fue incorporar un mecanismo de filtrado por popularidad directamente
en el script de procesamiento, controlado por las variables \texttt{FILTER\_TOP\_K\_POPULAR}
y \texttt{TOP\_K\_COUNT}, configuradas como se muestra en el
Código~\ref{lst:filtroPopularidad}.

\begin{lstlisting}[style=pythonStyle, caption=Configuración del filtro por popularidad, label=lst:filtroPopularidad]
FILTER_TOP_K_POPULAR = True
TOP_K_COUNT = 5000
\end{lstlisting}

Con este mecanismo activo, el script ordena las películas y series por su campo
\texttt{popularity} de la API de TMDB y retiene únicamente las \texttt{TOP\_K\_COUNT}
más populares antes de generar el \textit{dataset} de salida. Esto permitió trabajar durante el
desarrollo con un subconjunto representativo y manejable, manteniendo la opción de
escalar al \textit{dataset} completo simplemente desactivando el filtro o aumentando el límite.
La motivación de operar con un subconjunto y los detalles del script
de procesado se desarrollan en el
Apéndice~\ref{Appendix:integracion}.

    \subsection*{Valores numéricos y de fecha en los metadatos: la decisión de usar rangos}

Un obstáculo temprano en el diseño del filtrado fue la naturaleza de ciertos campos
de metadatos en TMDB: presupuesto, recaudación, duración, fecha de estreno. Almacenarlos
como valores exactos en la tabla \texttt{metadata} y filtrar por igualdad exacta habría
resultado inútil desde el punto de vista exploratorio: es improbable que un usuario quiera
películas con exactamente 157 minutos de duración o con un presupuesto de exactamente
80.000.000 de dólares.

La solución fue discretizar estos valores en rangos durante el procesamiento, antes de
insertarlos en la base de datos. Para valores numéricos, la función \texttt{bucket\_range}
agrupa cada valor en un intervalo de tamaño configurable, según la implementación
recogida en el Código~\ref{lst:bucketRange}.

\begin{lstlisting}[style=pythonStyle, caption={[Función \texttt{bucket\_range}]Función \texttt{bucket\_range} para discretizar valores numéricos}, label=lst:bucketRange]
def bucket_range(value, bucket_size):
    bucket_start = int(num // bucket_size) * bucket_size
    return f"{format_number(bucket_start)}-{format_number(bucket_start + bucket_size - 1)}"
\end{lstlisting}

Para fechas, se aplica una lógica análoga con granularidad configurable por año, mes y
día, definida en \texttt{format.json} por colección. De este modo, el valor almacenado en
\texttt{metadata} no es \texttt{157} sino \texttt{150-179}, lo que permite al sistema de
filtrado operar con igualdad de cadenas sobre rangos semánticamente útiles, sin necesidad
de soportar operadores de comparación numérica en las \textit{queries}. La alternativa de soportar
expresiones regulares en los filtros de metadatos fue considerada y descartada: la
complejidad añadida en la construcción de \textit{queries} y la dificultad de exponerlo de forma
intuitiva al usuario no compensaban el beneficio, siendo los rangos una solución más
simple y suficientemente expresiva para los casos de uso identificados. Para los casos en
que un usuario necesita combinar varios rangos, la operación de unión cubre esa necesidad
de forma más predecible.

    \subsection*{CORS y la comunicación entre \textit{frontend} y \textit{backend}}

El sistema está organizado como dos procesos independientes: el \textit{frontend} se sirve
estáticamente y realiza peticiones al \textit{backend} desplegado en Tomcat. Esta separación
implica que las peticiones HTTP se originan desde un origen distinto al del servidor de la
API, lo que activa la política de mismo origen (\textit{same-origin policy}) del navegador
y bloquea las respuestas por defecto.

El obstáculo no fue solo técnico sino también de comprensión: el mecanismo CORS
(\textit{Cross-Origin Resource Sharing}) no es transparente, y sus manifestaciones
---peticiones bloqueadas sin mensaje de error claro, o peticiones \texttt{OPTIONS}
inesperadas--- no resultan inmediatamente evidentes si no se ha trabajado previamente
con arquitecturas de \textit{frontend} y \textit{backend} separados. Fue necesario investigar el protocolo
para entender que el navegador envía primero una petición de preflight con método
\texttt{OPTIONS} antes de la petición real, y que el servidor debe responder con las
cabeceras adecuadas para que el navegador autorice la comunicación.

La solución fue implementar un filtro de servlet, \texttt{CORSFilter}, que intercepta
todas las peticiones mediante la anotación \texttt{@WebFilter("/*")} y añade las
cabeceras necesarias en cada respuesta, tal y como se reproduce en el
Código~\ref{lst:corsFilter}.

\begin{lstlisting}[style=javaStyle, caption={[Filtro \texttt{CORSFilter}]Filtro \texttt{CORSFilter} para gestionar las cabeceras CORS}, label=lst:corsFilter]
response.setHeader("Access-Control-Allow-Origin", origin);
response.setHeader("Access-Control-Allow-Credentials", "true");
response.setHeader("Access-Control-Allow-Methods",
    "GET, POST, PUT, DELETE, OPTIONS");

if ("OPTIONS".equalsIgnoreCase(request.getMethod())) {
    response.setStatus(HttpServletResponse.SC_OK);
    return;
}
chain.doFilter(req, res);
\end{lstlisting}

Un detalle relevante fue la combinación de \texttt{Access-Control-Allow-Credentials: true}
con el reflejo dinámico del origen en lugar de un comodín \texttt{*}: los navegadores
rechazan el uso de \texttt{*} cuando la petición incluye credenciales (en este caso,
la cookie de sesión), por lo que el filtro lee el encabezado \texttt{Origin} de la
petición y lo devuelve como valor explícito. Complementariamente, fue necesario
configurar el \texttt{CookieProcessor} en \texttt{context.xml} con
\texttt{sameSiteCookies="None"} para que la cookie de sesión se enviara correctamente
en peticiones cross-origin. La configuración exacta del contenedor de
servlets en los modos de desarrollo y de producción, junto con el
empaquetado del \textit{backend} tras un mismo origen mediante Nginx en
producción, se describe en el Apéndice~\ref{Appendix:despliegue}.

El flujo completo de una petición HTTP, desde el \textit{preflight}
CORS hasta la respuesta JSON, se ilustra en la
Figura~\ref{fig:httpFlow}. El diagrama distingue las dos fases que el
navegador desencadena de manera automática para una petición
cross-origin con credenciales: primero un \texttt{OPTIONS} que
\texttt{CORSFilter} corta en cuanto inyecta las cabeceras, y a
continuación la petición real, que recorre toda la cadena
(filtro $\to$ servlet $\to$ DAO $\to$ JDBC $\to$ PostgreSQL) antes de
devolver el cuerpo JSON.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
      node distance=0.55cm and 1.0cm,
      every node/.style={font=\footnotesize, align=center},
      block/.style={draw, rectangle, rounded corners,
                    minimum width=5.4cm, minimum height=0.75cm,
                    fill=blue!5},
      filter/.style={draw, rectangle, rounded corners,
                     minimum width=5.4cm, minimum height=0.75cm,
                     fill=orange!10},
      io/.style={draw, rectangle, rounded corners,
                 minimum width=5.4cm, minimum height=0.75cm,
                 fill=green!8},
      db/.style={draw, rectangle, rounded corners,
                 minimum width=5.4cm, minimum height=0.75cm,
                 fill=teal!8},
      arrow/.style={-Latex, thick}
    ]
    \node[io] (opt)
      {Cliente: \texttt{OPTIONS /api/\ldots}\\(preflight CORS)};
    \node[filter, below=of opt] (cors1)
      {\texttt{CORSFilter}: inyecta cabeceras\\
       \texttt{Access-Control-Allow-*} y responde \texttt{200}};
    \node[io, below=of cors1] (req)
      {Cliente: petición real (\texttt{GET}/\texttt{POST} /api/\ldots)\\
       con \texttt{Cookie: JSESSIONID}};
    \node[filter, below=of req] (cors2)
      {\texttt{CORSFilter}: añade cabeceras CORS\\
       y delega con \texttt{chain.doFilter()}};
    \node[block, below=of cors2] (route)
      {Tomcat enruta al \texttt{Servlet} por \texttt{@WebServlet}};
    \node[block, below=of route] (session)
      {\texttt{req.getSession()} $\to$\\
       \texttt{(StateManager) session.getAttribute("navState")}};
    \node[block, below=of session] (dao)
      {\texttt{Servlet} $\to$ \texttt{NavigationDAO} con el estado};
    \node[db, below=of dao] (db)
      {\texttt{NavigationDAO} $\to$ JDBC (\texttt{PreparedStatement})\\
       $\to$ PostgreSQL};
    \node[io, below=of db] (resp)
      {\texttt{Servlet} serializa con Jackson\\
       $\to$ \texttt{HTTP 200} \texttt{application/json}};

    \draw[arrow] (opt) -- (cors1);
    \draw[arrow] (cors1) -- node[right]{respuesta vacía} (req);
    \draw[arrow] (req) -- (cors2);
    \draw[arrow] (cors2) -- (route);
    \draw[arrow] (route) -- (session);
    \draw[arrow] (session) -- (dao);
    \draw[arrow] (dao) -- (db);
    \draw[arrow] (db) -- (resp);
  \end{tikzpicture}
  \caption[Flujo de una petición HTTP]{Flujo completo de una petición
  HTTP en el sistema. La fase de \textit{preflight} (parte superior) se
  resuelve íntegramente en \texttt{CORSFilter}, sin alcanzar al servlet.
  La petición real (parte inferior) atraviesa el filtro, el routing por
  anotación, la recuperación del \texttt{StateManager} en la sesión HTTP,
  la consulta a través de \texttt{NavigationDAO} y JDBC, y termina con
  una respuesta JSON serializada por Jackson.}
  \label{fig:httpFlow}
\end{figure}

    \subsection*{El diseño de \texttt{goback} y sus implicaciones}

La operación \texttt{goback} fue uno de los puntos del diseño que requirió más
iteraciones. La dificultad radicó en que revertir una transición entre colecciones no
es simplemente volver a la colección anterior: además de restaurar el estado concreto
que existía antes de la transición, con todos los filtros que estaban activos en ese
momento, hay que restringir los objetos de la colección de destino a aquellos que
apuntan a los elementos actualmente seleccionados, preservando así la coherencia del
recorrido relacional.

Un primer enfoque consistió en eliminar el último \texttt{Link} del historial al
ejecutar \texttt{goback}, restaurando la colección activa a la del enlace eliminado.
Este enfoque resultó problemático porque el historial perdía información: si el usuario
navegaba hacia adelante, retrocedía y volvía a navegar, la cadena de transiciones no
reflejaba fielmente el recorrido real, y las subconsultas generadas por
\texttt{appendFilterClauses} producían resultados incorrectos.

La solución adoptada fue convertir \texttt{goback} en una operación aditiva sobre el
historial: en lugar de borrar el último \texttt{Link}, añade uno nuevo a la lista de
transiciones del estado con la dirección invertida (\texttt{forward = false}),
preservando así el historial completo de forma creciente. Para saber qué transición debe
revertirse en cada \texttt{goback}, \texttt{StateManager} mantiene una pila auxiliar,
\texttt{linkStack}, que solo registra los enlaces hacia delante creados por
\texttt{link()}; \texttt{goback} extrae el último elemento de esa pila para conocer la
colección de origen y la razón del salto que se va a deshacer, dejando el historial
real, residente en \texttt{cur.getLinks()}, intacto y monótonamente creciente. La
implementación final coincide con la mostrada en el Código~\ref{lst:goback}.

La consecuencia de este diseño, descrita en la sección~\ref{sec:navegacion_transversal},
es que \texttt{appendFilterClauses} debe recorrer toda la cadena acumulada, incluidos los
retrocesos, para construir correctamente las subconsultas anidadas. El coste es una \textit{query}
potencialmente más larga conforme el usuario navega y retrocede, pero garantiza que el
contexto de exploración se reconstruye de forma exacta en cada petición.